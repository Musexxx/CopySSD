# ESC键兜底机制实现说明

## 功能概述

实现了一个全局ESC键监听机制，即使在窗口失去焦点的情况下，也能通过按ESC键强制关闭报警窗口。这是一个**兜底机制**，用于解决极端情况下窗口无法点击的问题。

## 实现原理

### 1. 全局事件过滤器
- 使用Qt的`QApplication::installEventFilter()`在应用级别安装事件过滤器
- 监听所有键盘事件，特别捕获ESC键
- **即使窗口失去焦点，也能捕获ESC键**

### 2. 活动窗口追踪
- 使用静态列表`s_activeDialogs`追踪所有活动的报警窗口
- 当窗口创建时，自动添加到列表
- 当窗口关闭时，自动从列表移除

### 3. 优先级处理
- 优先处理模态窗口（`OverflowAlert`和`MaterialConfirm`）
- 如果同时有多个窗口，优先关闭模态窗口

## 工作流程

```
1. 报警窗口创建
   ↓
2. installGlobalEventFilter()
   - 添加到s_activeDialogs列表
   - 如果是第一个窗口，安装全局事件过滤器
   ↓
3. 用户按ESC键（无论窗口是否有焦点）
   ↓
4. eventFilter()捕获ESC键事件
   ↓
5. 查找最顶层的活动窗口
   ↓
6. 调用onCloseClicked()或直接close()
   ↓
7. 窗口关闭 → closeEvent() → removeGlobalEventFilter()
   ↓
8. 从s_activeDialogs移除，如果列表为空则移除事件过滤器
```

## 使用场景

### ✅ 正常情况
- 窗口有焦点：ESC键通过`keyPressEvent()`正常处理
- 窗口无焦点但可见：ESC键通过`eventFilter()`全局捕获并强制关闭

### ✅ 极端情况（兜底）
- 窗口完全失去焦点，无法点击按钮
- 系统焦点管理异常
- 其他程序窗口覆盖报警窗口
- **解决方案：按ESC键强制关闭**

## 代码实现

### 关键方法

1. **installGlobalEventFilter()**
   - 在构造函数中调用
   - 将窗口添加到活动列表
   - 安装全局事件过滤器

2. **removeGlobalEventFilter()**
   - 在`closeEvent()`、`onCloseClicked()`、`onAutoClose()`中调用
   - 从活动列表移除窗口
   - 如果列表为空，移除全局事件过滤器

3. **eventFilter(QObject* obj, QEvent* event)**
   - 全局事件过滤器实现
   - 捕获ESC键事件
   - 查找并关闭最顶层的活动窗口

4. **closeEvent(QCloseEvent* event)**
   - 确保窗口被任何方式关闭时都能清理事件过滤器

## 优势

1. **可靠性高**：即使窗口失去焦点也能响应
2. **自动管理**：窗口创建/销毁时自动安装/移除过滤器
3. **资源友好**：只有活动窗口时才安装过滤器
4. **优先级处理**：模态窗口优先关闭
5. **不影响正常功能**：窗口有焦点时仍通过正常流程处理

## 注意事项

1. **事件过滤器共享**：多个窗口共享同一个事件过滤器实例
2. **清理机制**：确保所有关闭路径都调用`removeGlobalEventFilter()`
3. **线程安全**：当前实现是单线程的，如果多线程需要加锁

## 测试建议

### 测试场景1：窗口有焦点
- 触发报警窗口
- 按ESC键
- **预期**：窗口正常关闭（通过`keyPressEvent()`）

### 测试场景2：窗口失去焦点
- 触发报警窗口
- 切换到其他窗口（或Alt+Tab）
- 按ESC键
- **预期**：报警窗口被强制关闭（通过`eventFilter()`）

### 测试场景3：多个窗口
- 同时触发溢出报警和料号确认
- 按ESC键
- **预期**：优先关闭模态窗口

### 测试场景4：极端情况
- 触发报警窗口
- 使用其他程序覆盖窗口
- 按ESC键
- **预期**：报警窗口被强制关闭（兜底机制生效）

## 结论

✅ **可以实现**：ESC键兜底机制已成功实现
✅ **窗口失去焦点时生效**：全局事件过滤器可以捕获ESC键，即使窗口失去焦点
✅ **不影响正常功能**：窗口有焦点时仍使用正常的按键处理流程
✅ **自动管理**：窗口创建/销毁时自动管理事件过滤器

这个机制大大提高了报警窗口的可靠性，即使在极端情况下也能通过ESC键强制关闭窗口。

