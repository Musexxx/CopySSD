# 模态窗口影响和可行性分析

## 一、影响分析

### 1. 料号确认窗口（MaterialConfirm）

**触发位置**：
- `processLogFile()` → `validateMaterialCode()` → `showMaterialCodeConfirmationDialog()`
- 在主线程的定时器（每100ms）中调用

**改为模态的影响**：
- ✅ **优点**：确保窗口获得焦点，按钮可以点击
- ❌ **缺点**：
  - 阻塞主线程，文件处理队列暂停
  - UI界面冻结，无法操作主窗口
  - 其他定时器任务暂停（同步、监控等）

**缓解因素**：
- 已有 `pendingOrderQueue` 机制，等待确认时跳过处理
- 文件已移到缓存目录，不会丢失
- 用户通常很快响应（几秒内）

**结论**：**影响可控**，但会暂停文件处理

---

### 2. 溢出报警窗口（OverflowAlert）

**触发位置**：
- `updateRow()` / `updateWorkOrderCount()` / `syncAllWorkOrderCounts()`
- 在同步定时器（30秒）和文件监控回调中调用

**改为模态的影响**：
- ✅ **优点**：
  - 确保用户必须处理溢出问题
  - 窗口获得焦点，按钮可以点击
- ❌ **缺点**：
  - 阻塞主窗口更新
  - 阻塞文件处理队列
  - 阻塞其他定时器任务

**缓解因素**：
- 溢出报警本身就需要用户立即处理，阻塞是合理的
- 单例模式保证只有一个窗口，不会叠加阻塞

**结论**：**改为模态合理且必要**

---

### 3. NUM.txt文件操作失败报警（FileOperationError）

**触发位置**：
- `onAddWorkOrder()` - 用户手动添加工单
- `onModifyTotalCount()` - 用户手动修改总量

**改为模态的影响**：
- ✅ **优点**：确保用户看到错误信息
- ❌ **缺点**：几乎无影响（用户操作场景）

**结论**：**改为模态完全合理**

---

### 4. 工单状态异常报警（AnomalyAlert）

**当前状态**：已被注释，暂不考虑

---

## 二、可行性分析

### 方案对比

| 方案 | 料号确认 | 溢出报警 | NUM.txt错误 | 优点 | 缺点 |
|------|---------|---------|------------|------|------|
| **方案A：全部模态** | 模态 | 模态 | 模态 | 简单可靠，确保焦点 | 料号确认会阻塞文件处理 |
| **方案B：部分模态** | 非模态 | 模态 | 模态 | 平衡性能和可靠性 | 料号确认仍可能有问题 |
| **方案C：exec()混合** | exec() | exec() | exec() | 保持现有逻辑，确保焦点 | 需要修改调用方式 |

---

## 三、推荐方案

### 🎯 **推荐：方案B（部分模态）**

**实施策略**：
1. **溢出报警** → 改为模态（`setModal(true)`）
2. **NUM.txt错误** → 改为模态（`setModal(true)`）
3. **料号确认** → 保持非模态，但：
   - 使用 `exec()` 而非 `show()`（如果用户响应）
   - 或者保持 `show()`，但改进激活逻辑

**理由**：
- 溢出报警和NUM.txt错误需要用户立即处理，阻塞是合理的
- 料号确认如果频繁出现，阻塞会影响文件处理效率
- 但料号确认已有队列机制保护，改为模态也可接受

---

### 🔧 **备选方案：全部模态（如果方案B仍有问题）**

如果料号确认保持非模态仍无法点击，可以全部改为模态：

**实施**：
```cpp
// 所有窗口类型都改为模态
setModal(true);
setWindowModality(Qt::ApplicationModal);  // 阻塞整个应用
```

**影响**：
- 文件处理会暂停，但已有队列机制保护
- 用户响应后继续处理，不会丢失数据

---

## 四、实施建议

### 优先级1：溢出报警和NUM.txt错误改为模态
- 这些场景必须确保用户处理
- 阻塞影响小，可以接受

### 优先级2：料号确认保持非模态但改进
- 如果改进后仍无法点击，再改为模态
- 可以先测试当前改进方案的效果

### 优先级3：如果仍有问题，全部改为模态
- 最后手段，确保可用性优先

---

## 五、风险评估

### 低风险 ✅
- 溢出报警改为模态
- NUM.txt错误改为模态

### 中风险 ⚠️
- 料号确认改为模态（会影响文件处理效率）

### 应对措施
- 如果改为模态，确保：
  1. 用户界面有明确提示
  2. 文件处理队列有保护机制（已有）
  3. 日志记录完整，便于排查

