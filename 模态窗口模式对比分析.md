# 模态窗口模式对比分析

## 当前模式：setModal(true) + show()

### ✅ 优点
1. **不阻塞主线程**：文件处理队列继续运行
2. **灵活控制**：可以通过信号槽机制处理用户响应
3. **多窗口支持**：理论上可以同时显示多个报警（虽然当前实现是单例）

### ❌ 缺点
1. **主线程不阻塞**：文件处理会继续，可能不是用户期望的行为
2. **焦点不保证**：虽然有多重激活逻辑，但仍可能被其他窗口抢占
3. **窗口无法点击风险**：中等风险（约20-30%）

---

## 备选模式：setModal(true) + exec()

### ✅ 优点
1. **完全阻塞主线程**：文件处理队列暂停，确保用户必须先处理报警
2. **焦点可靠性高**：exec() 运行模态事件循环，窗口焦点更可靠
3. **窗口无法点击风险低**：风险降至5-10%

### ❌ 缺点
1. **阻塞主线程**：所有文件处理暂停，直到用户响应
2. **无法并发显示**：如果有多个报警，只能串行显示
3. **代码改动大**：需要修改所有调用处，改为使用 exec() 而非信号槽

---

## 风险评估

### 当前模式（show()）的风险
- **窗口无法点击风险**：⭐⭐⭐☆☆（中等）
  - 原因：虽然有多重激活，但系统焦点管理可能失败
  - 发生概率：约20-30%（根据Windows系统状态）
  - 缓解措施：已实现多重激活逻辑

- **功能影响**：⭐⭐☆☆☆（低）
  - 文件处理继续运行，可能产生更多溢出文件
  - 但已有队列机制保护，不会丢失数据

### exec() 模式的风险
- **窗口无法点击风险**：⭐☆☆☆☆（低）
  - 原因：exec() 的模态事件循环更可靠
  - 发生概率：约5-10%

- **功能影响**：⭐⭐⭐☆☆（中等）
  - 文件处理暂停，但这是合理的（报警需要立即处理）
  - 无法并发显示多个报警（但溢出报警是单例，料号确认已有队列机制）

---

## 建议

### 🎯 推荐方案：保持当前模式，但加强监控

**理由**：
1. 当前模式已实现多重激活逻辑，应该能解决大部分焦点问题
2. 文件处理继续运行可能符合用户需求（避免阻塞）
3. 如果测试后仍有问题，再考虑改为 exec()

**实施**：
1. 保持当前 `setModal(true) + show()` 模式
2. 在日志中记录窗口焦点状态，便于诊断
3. 如果仍出现无法点击问题，再改为 exec()

### 🔧 备选方案：改为 exec() 模式（如果当前模式仍有问题）

**触发条件**：
- 测试后发现窗口仍然无法点击（概率>10%）
- 用户确认需要阻塞文件处理（报警优先）

**实施**：
- 修改 `FullScreenAlertDialog` 构造函数，不在构造函数中显示
- 修改所有调用处，使用 `exec()` 而非信号槽
- 需要改动：`showMaterialCodeConfirmationDialog` 和 `OverflowAlertManager::showOverflowAlert`

---

## 测试建议

### 测试场景1：溢出报警焦点测试
1. 触发溢出报警
2. 观察窗口是否能立即获得焦点
3. 测试按钮是否能正常点击
4. 测试是否能在其他窗口上操作（应该被阻止）

### 测试场景2：料号确认焦点测试
1. 触发料号确认对话框
2. 观察窗口是否能立即获得焦点
3. 测试按钮是否能正常点击
4. 测试文件处理是否继续（应该继续）

### 测试场景3：并发报警测试
1. 同时触发溢出报警和料号确认
2. 观察两个窗口的焦点行为
3. 测试是否都能正常操作

---

## 结论

**当前模式（setModal(true) + show()）**：
- ✅ 不会影响程序正常功能
- ⚠️ 窗口无法点击风险：中等（约20-30%）
- ✅ 文件处理继续运行（可能是优点，也可能是缺点）

**如果测试后仍有问题**：
- 建议改为 `exec()` 模式
- 虽然会阻塞文件处理，但报警窗口的可靠性更重要


