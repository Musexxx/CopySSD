# copyssd 代码设计改进指南

> 本文档面向首次独立完成完整项目的开发者，结合 copyssd 的实际代码，系统性地讲解软件设计原则、常见问题、改进方法，以及测试相关概念。

---

## 目录

1. [前言：你做了什么，以及这代表什么](#一前言你做了什么以及这代表什么)
2. [核心概念速览](#二核心概念速览)
3. [问题一：巨型上帝类](#三问题一巨型上帝类)
4. [问题二：代码重复](#四问题二代码重复)
5. [问题三：巨型方法](#五问题三巨型方法)
6. [问题四：紧耦合与硬编码](#六问题四紧耦合与硬编码)
7. [问题五：遗留死代码](#七问题五遗留死代码)
8. [单元测试与集成测试详解](#八单元测试与集成测试详解)
9. [设计原则清单](#九设计原则清单)
10. [渐进式重构路线图](#十渐进式重构路线图)

---

## 一、前言：你做了什么，以及这代表什么

### 这是一个怎样的项目

copyssd 是一个**运行在真实产线上的工单管理系统**，处理文件监控、条码验证、料号审核、溢出报警、中控通信、拷贝机控制。它不是课堂作业或 demo——它解决的是工厂里每天发生的实际问题。

### 你已经做到了什么（请先肯定自己）

- **工程可靠性**：重试机制、文件锁、冷却防抖、ESC 兜底——这些都是从实际踩坑中长出来的经验，教科书不会教你
- **业务理解深入**：三级归档（根目录/completed/overlog）、料号白黑名单、溢出判断逻辑——你理解产线需要什么
- **平台细节处理**：Windows UNC 路径 `\\` 被 QSettings 转成 `\` 的坑、`SetForegroundWindow` 焦点争夺——这些不是看书能学会的
- **独立交付**：从设计、编码、调试到部署维护全是一个人完成

### 本文档的目的

上面的评价说明你有很好的**工程直觉**，但缺乏系统的**软件设计训练**。本文档将：

1. 用你的实际代码指出具体问题
2. 解释背后的设计原则
3. 给出可操作的改进示例
4. 帮助你从"能跑"走到"好维护"

每一条批评都是针对**代码结构**而非**你个人**。每个写了几年程序的开发者回头看自己的早期代码都会有同感——这是成长的必经之路。

---

## 二、核心概念速览

在深入具体问题之前，先建立几个关键概念的直觉理解。

### 什么是"好的代码设计"？

用一句话概括：**改一个功能时，只需要改一处代码；加一个功能时，不需要改已有代码。**

如果每次加新功能你都在 MainWindow 里塞新方法、新成员变量、新 if-else 分支，那就说明设计有问题。

### 核心概念对照表

| 原则 | 一句话解释 | 你的代码中的反面案例 |
|------|-----------|---------------------|
| **单一职责原则 (SRP)** | 一个类只做一件事 | MainWindow 做了 20+ 件事 |
| **DRY 原则** | 不要重复自己 | 网络路径构造逻辑复制了 15 遍 |
| **开闭原则 (OCP)** | 扩展开放，修改关闭 | 加新报警类型要改 MainWindow、FullScreenAlertDialog |
| **依赖注入 (DI)** | 不要让类自己找依赖 | MainWindow 内部直接读 config.ini |
| **关注点分离** | UI 代码和业务逻辑分开 | processLogFile 里既有文件操作又有弹窗 |

### 类和对象的关系

目前你只有一个真正意义上的业务类 `WorkOrder`——它只存数据。其他所有逻辑全部堆在 `MainWindow` 里。

理想情况下，你的程序应该由多个各司其职的类组成：

```
┌─────────────┐  ┌──────────────┐  ┌─────────────┐  ┌──────────────┐
│ FileProcessor│  │ OrderManager │  │ AlertManager │  │ ConfigLoader │
│  文件处理    │  │  工单管理    │  │  报警管理    │  │  配置加载    │
└─────────────┘  └──────────────┘  └─────────────┘  └──────────────┘
       │                │                 │                │
       └────────────────┼─────────────────┼────────────────┘
                        │                 │
                   ┌────▼─────────────────▼────┐
                   │        MainWindow         │
                   │    (只负责 UI 组装和协调)   │
                   └───────────────────────────┘
```

MainWindow 应该像一个**乐队指挥**——它知道有哪些乐手（类），知道什么时候让谁演奏（调用），但它自己不会去弹钢琴、拉小提琴、吹萨克斯。

---

## 三、问题一：巨型上帝类

### 3.1 什么是上帝类 (God Class)

上帝类是一个知道太多、做太多的类。在面向对象设计中，这是最常见的反模式（anti-pattern）。它的特征是：

- 文件行数巨大（你的 mainwindow.cpp 约 5600 行）
- 成员变量几十个
- 方法上百个
- 包含了 UI、业务逻辑、数据访问、网络通信等所有职责

### 3.2 你的代码现状

`MainWindow` 当前同时负责：

```
MainWindow 的职责清单（不完全统计）
├── UI 构建与更新 (setupUI, setupDualTableUI, updateTable, updateRow...)
├── 配置加载 (loadConfig, createDefaultConfig)
├── 文件监控 (onDirectoryChanged, processDirectoryFiles)
├── 队列处理 (processNextFile, addToProcessQueue)
├── 日志文件解析 (processLogFile, validateBarcode)
├── 文件移动 (moveLogFile, ensureLogExtension)
├── 重复检测 (getNextDuplicateNumber)
├── 溢出检测与报警 (showOverflowAlert, getOverflowCount)
├── 工单 CRUD (onAddWorkOrder, onDeleteWorkOrder, onModifyTotalCount)
├── NUM.txt 读写 (readWorkOrderTotalFromNumFile, writeWorkOrderTotalToNumFile)
├── 文件锁 (tryLockConfigFile, unlockConfigFile)
├── 料号验证 (validateMaterialCode, loadOrderMaterialWhitelist...)
├── 料号缓存管理 (moveFileToMaterialCache, cleanupMaterialCache...)
├── 时间记录 (writeTimeRecord)
├── WebSocket 通信 (setupWebSocket, sendAllWorkOrdersToCentral...)
├── 工单持久化 (saveWorkOrders, askToLoadSavedWorkOrders...)
├── 通知管理 (shouldShowNotification, cleanupExpiredNotifications...)
├── 大小校验 (maybeStartAsyncSizeValidation, onSizeValidationFinished)
├── 网络路径工具 (isNetworkPath, checkNetworkPathAccessibility...)
├── 表格行操作 (moveRowToTop, moveRowToBottom...)
└── 快捷添加 (onQuickAdd, addQuickAddButton)
```

这就是上帝类——所有功能都塞在一个篮子里。

### 3.3 问题后果

1. **改一处可能坏十处**：修改文件移动逻辑可能意外影响 WebSocket 通信
2. **无法复用**：如果另一个窗口也需要文件处理能力，你只能复制粘贴代码
3. **无法独立测试**：要测试 `processLogFile`，你必须构造一个完整的 `MainWindow`（包括 UI、WebSocket、文件监控……）
4. **多人协作困难**：两个人不能同时修改不同功能，因为都在同一个文件里
5. **理解成本高**：新接手的人需要读完 5600 行才能开始改代码

### 3.4 改进方案：按职责拆分

#### 第一步：识别职责边界

你可以按业务领域拆分为以下几个独立的类：

| 新类名 | 职责 | 从 MainWindow 移出的内容 |
|--------|------|------------------------|
| `FileProcessor` | 文件读取、解析、验证 | `processLogFile`, `validateBarcode`, `ensureLogExtension` |
| `FileMover` | 文件移动、重复检测、溢出判断 | `moveLogFile`, `getNextDuplicateNumber`, `checkAndCreateOrderFolder` |
| `OrderManager` | 工单 CRUD、计数、持久化 | `onAddWorkOrder`, `onDeleteWorkOrder`, `updateWorkOrderCount`, `saveWorkOrders`, `loadSavedWorkOrders` |
| `MaterialValidator` | 料号白/黑名单、确认流程 | `validateMaterialCode`, `loadOrderMaterialWhitelist`, 等全部料号相关 |
| `AlertService` | 报警与通知管理 | `showOverflowAlert`, `showNotification`, `shouldShowNotification`, `OverflowAlertManager` |
| `ConfigManager` | 配置读取与路径管理 | `loadConfig`, `createDefaultConfig`, `isNetworkPath`, `buildArchivePath` |
| `WebSocketManager` | 中控通信 | `setupWebSocket`, `sendAllWorkOrdersToCentral`, 自动重连逻辑 |
| `TimeRecordWriter` | 时间记录写入 | `writeTimeRecord` |
| `SizeValidator` | 异步大小校验 | `maybeStartAsyncSizeValidation`, `parseField5AsCopySizeMiB` 等工具函数 |

#### 第二步：示例——拆分 FileProcessor

**现状（mainwindow.cpp 中的巨型类）**：
```cpp
// 现在：所有逻辑在 MainWindow 里
class MainWindow : public QMainWindow
{
    // ... 80+ 个成员变量 ...
    // ... 200+ 个方法 ...
    
    bool processLogFile(const QString& filePath);
    bool validateBarcode(const QString& filePath, const QString& fileContent);
    void handleMismatchedFile(const QString& filePath);
    void handleErrorFile(const QString& filePath, ...);
    // ... 所有其他方法 ...
};
```

**改进后**：
```cpp
// fileprocessor.h —— 独立的文件处理类
class FileProcessor : public QObject
{
    Q_OBJECT

public:
    // 依赖通过构造函数注入，而不是让这个类自己去"找"依赖
    explicit FileProcessor(MaterialValidator* materialValidator,
                           FileMover* fileMover,
                           TimeRecordWriter* recordWriter,
                           QObject* parent = nullptr);

    // 只有一个公开方法：处理一个文件，返回是否成功
    bool process(const QString& filePath);

signals:
    void fileProcessed(const QString& orderNumber, const QString& barcode);
    void fileError(const QString& filePath, const QString& reason);
    void barcodeMismatch(const QString& filePath);
    void materialPending(const QString& orderNumber, const QString& materialCode);

private:
    bool validateBarcode(const QString& filePath, const QString& fileContent);
    bool readFileContent(const QString& filePath, QString& content);

    // 依赖对象（由外部注入，不是自己 new）
    MaterialValidator* m_materialValidator;
    FileMover*         m_fileMover;
    TimeRecordWriter*  m_recordWriter;
};
```

```cpp
// fileprocessor.cpp
bool FileProcessor::process(const QString& filePath)
{
    // 1. 读文件内容
    QString content;
    if (!readFileContent(filePath, content))
        return false;

    // 2. 验证条码
    if (!validateBarcode(filePath, content)) {
        emit barcodeMismatch(filePath);
        return false;
    }

    // 3. 解析工单号、条码、料号
    QStringList parts = content.split("|");
    QString orderNumber = parts[0].trimmed();
    QString barcode     = parts[1].trimmed();
    QString material    = (parts.size() >= 3) ? parts[2].trimmed() : QString();

    // 4. 料号验证（委托给专门的对象，不是自己处理）
    if (!m_materialValidator->validate(orderNumber, barcode, material, filePath)) {
        emit materialPending(orderNumber, material);
        return false;
    }

    // 5. 获取文件创建时间
    QFileInfo fi(filePath);
    QString createTime = fi.birthTime().toString("yyyy-MM-dd hh:mm:ss.zzz");

    // 6. 移动文件（委托）
    int copyCount = 1;
    if (!m_fileMover->moveToArchive(filePath, orderNumber, barcode, copyCount))
        return false;

    // 7. 写时间记录（委托）
    m_recordWriter->write(orderNumber, barcode, createTime, copyCount);

    emit fileProcessed(orderNumber, barcode);
    return true;
}
```

**关键变化**：
- `MainWindow` 不再处理文件解析的细节，它只调用 `m_fileProcessor->process(filePath)`
- `FileProcessor` 不需要知道 UI 的存在——它通过信号通知结果，由 `MainWindow` 决定如何展示
- `MaterialValidator`、`FileMover`、`TimeRecordWriter` 等依赖通过**构造函数注入**，而不是在内部 `new` 或访问全局单例
- 如果将来换了文件格式，只需要改 `FileProcessor`，MainWindow 一行不动

#### 第三步：MainWindow 变为协调者

```cpp
// mainwindow.h —— 拆分后的 MainWindow，只负责 UI 和协调
class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    explicit MainWindow(QWidget* parent = nullptr);

private slots:
    void onAddWorkOrder();
    void onRefresh();
    void onConnectButtonClicked();
    void onFileProcessed(const QString& orderNumber, const QString& barcode);
    void onFileError(const QString& filePath, const QString& reason);

private:
    void setupUI();

    // UI 组件
    QTableWidget* m_incompleteTable;
    QTableWidget* m_completedTable;
    QPushButton*  m_connectButton;

    // 业务对象（组合，不是继承）
    FileProcessor*    m_fileProcessor;
    OrderManager*     m_orderManager;
    ConfigManager*    m_configManager;
    WebSocketManager* m_wsManager;
    AlertService*     m_alertService;
};
```

MainWindow 的构造变成这样：
```cpp
MainWindow::MainWindow(QWidget* parent)
    : QMainWindow(parent)
{
    // 按依赖顺序创建对象
    m_configManager   = new ConfigManager(this);
    m_orderManager    = new OrderManager(m_configManager, this);
    m_wsManager       = new WebSocketManager(m_configManager, this);
    m_alertService    = new AlertService(this);
    m_fileProcessor   = new FileProcessor(
                           new MaterialValidator(m_configManager, this),
                           new FileMover(m_configManager, m_alertService, this),
                           new TimeRecordWriter(m_configManager, this),
                           this
                       );

    setupUI();
    connectSignals();  // 连接各对象的信号到 MainWindow 的槽
}
```

这个构造函数清晰地展示了程序的**依赖拓扑**——谁依赖谁一目了然。

---

## 四、问题二：代码重复

### 4.1 什么是 DRY 原则

**DRY = Don't Repeat Yourself（不要重复自己）**

代码重复是最容易识别也最危险的问题。当你需要修改一个逻辑时，如果这个逻辑在 15 个地方都出现了一遍，你就必须改 15 遍——漏掉任何一处都是 bug。

### 4.2 你的代码中最严重的重复

网络路径构造逻辑在你的代码中出现了**至少 15 次**。以下是几个典型的出现位置：

`writeTimeRecord()` 中（mainwindow.cpp:581-595）:
```cpp
if (isNetworkPath(archivePath)) {
    if (archivePath.startsWith("\\")) {
        orderPath = archivePath + "\\" + orderNumber;
        timeFilePath = orderPath + "\\TimeCount" + machineId + ".txt";
    } else if (archivePath.startsWith("\\\\")) {
        orderPath = archivePath + "\\" + orderNumber;
        timeFilePath = orderPath + "\\TimeCount" + machineId + ".txt";
    } else {
        orderPath = archivePath + "/" + orderNumber;
        timeFilePath = orderPath + "/TimeCount" + machineId + ".txt";
    }
} else {
    QDir archiveDir(archivePath);
    orderPath = archiveDir.filePath(orderNumber);
    timeFilePath = QDir(orderPath).filePath(QString("TimeCount%1.txt").arg(machineId));
}
```

`moveLogFile()` 中（mainwindow.cpp:3149-3169）:
```cpp
if (isNetworkPath(archivePath)) {
    if (archivePath.startsWith("\\")) {
        orderPath = archivePath + "\\" + orderNumber;
        destFile = orderPath + "\\" + targetFileName;
    } else if (archivePath.startsWith("\\\\")) {
        orderPath = archivePath + "\\" + orderNumber;
        destFile = orderPath + "\\" + targetFileName;
    } else {
        orderPath = archivePath + "/" + orderNumber;
        destFile = orderPath + "/" + targetFileName;
    }
} else {
    QDir archiveDir(archivePath);
    orderPath = archiveDir.filePath(orderNumber);
    destFile = QDir(orderPath).filePath(targetFileName);
}
```

`setupWorkOrderMonitoring()` 中（mainwindow.cpp:2863-2876）:
```cpp
if (isNetworkPath(archivePath)) {
    if (archivePath.startsWith("\\")) {
        orderPath = archivePath + "\\" + orderNumber;
    } else if (archivePath.startsWith("\\\\")) {
        orderPath = archivePath + "\\" + orderNumber;
    } else {
        orderPath = archivePath + "/" + orderNumber;
    }
} else {
    QDir archiveDir(archivePath);
    orderPath = archiveDir.filePath(orderNumber);
}
```

还有 `onRefresh()`、`readWorkOrderTotalFromNumFile()`、`writeWorkOrderTotalToNumFile()`、`moveFilesFromCacheToArchive()` 等等地方都是一模一样的模式。

### 4.3 为什么会出现这个问题

根本原因：**当你第一次写这段网络路径构造逻辑时，它是放在 `writeTimeRecord()` 函数里的。后来每次需要用到"构造归档路径"这个功能时，你找到上次写的代码，复制粘贴到新位置，然后微调。**

这个模式叫 **Copy-Paste Programming**，是所有开发者都经历过的阶段。问题是：

- 假设有一天你发现路径构造有一个 bug（比如对 `startsWith("\\")` 的判断和 `startsWith("\\\\")` 的处理完全一样，这个分支是无用的），你需要改 15 个地方
- 更实际的是：这两个分支确实一样——说明你当时不确定该用哪个，就两个都写了，然后到处复制

### 4.4 改进方案：提取公共方法

```cpp
// configmanager.h —— 路径构造统一放在配置管理类中
class ConfigManager : public QObject
{
    Q_OBJECT

public:
    // ... loadConfig, createDefaultConfig ...

    // 统一的路径构造方法（消除所有重复）
    QString buildArchivePath(const QString& orderNumber) const;
    QString buildArchiveFilePath(const QString& orderNumber, const QString& fileName) const;
    QString buildWorkOrderFolder(const QString& orderNumber) const;
    QString buildTimeCountFilePath(const QString& orderNumber) const;
    QString buildDuplicateOrderPath(const QString& orderNumber) const;

    // 通用路径拼接（处理网络路径的复杂性）
    QString joinPath(const QString& base, const QString& sub) const;

    // 统一的文件分隔符选择
    QString pathSeparator() const;

private:
    QString m_archivePath;
    QString m_duplicatePath;
    QString m_watchPath;
    QString m_machineId;
    // ... 其他配置项 ...
};
```

```cpp
// configmanager.cpp
QString ConfigManager::buildArchivePath(const QString& orderNumber) const
{
    return joinPath(m_archivePath, orderNumber);
}

QString ConfigManager::buildTimeCountFilePath(const QString& orderNumber) const
{
    return joinPath(buildArchivePath(orderNumber),
                    QString("TimeCount%1.txt").arg(m_machineId));
}

// 核心：所有路径拼接都走这一个方法，网络路径的复杂性只处理一次
QString ConfigManager::joinPath(const QString& base, const QString& sub) const
{
    if (isNetworkPath(base)) {
        // 统一使用反斜杠拼接 Windows 网络路径
        return base + QLatin1String("\\") + sub;
    }
    // 本地路径使用 Qt 标准方法
    return QDir(base).filePath(sub);
}
```

**效果**：原来分散在 15 个函数中的路径拼接逻辑，现在只需要一行：

```cpp
// 之前（复制粘贴了 15 次）
QString orderPath;
if (isNetworkPath(archivePath)) {
    if (archivePath.startsWith("\\")) {
        orderPath = archivePath + "\\" + orderNumber;
    } else if (archivePath.startsWith("\\\\")) {
        orderPath = archivePath + "\\" + orderNumber;
    } else {
        orderPath = archivePath + "/" + orderNumber;
    }
} else {
    QDir archiveDir(archivePath);
    orderPath = archiveDir.filePath(orderNumber);
}

// 之后（一行调用）
QString orderPath = m_configManager->buildArchivePath(orderNumber);
```

如果路径逻辑需要修改（比如网络路径统一改用 `/` 分隔），你只需要修改 `joinPath()` 这一个方法。

---

## 五、问题三：巨型方法

### 5.1 什么是巨型方法 (Long Method)

一个方法做太多事情，导致难以理解、难以测试、难以修改。

### 5.2 典型例子：`moveLogFile()`

这个函数从 mainwindow.cpp:3123 到 3443 行，约 **320 行**。它干了这些事：

1. 检查源文件是否存在
2. 检查并创建工单文件夹
3. 构造目标路径（含网络路径处理）
4. 检测根目录是否有重复
5. 检测 completed 文件夹是否有重复
6. 检测 overlog 文件夹是否有重复
7. 处理重复文件：创建 duplicate 目录、获取序号、构造新文件名、移动旧文件
8. 判断溢出：统计文件数 vs 总数，创建 overlog 目录
9. 触发溢出报警
10. 移动新文件到目标位置
11. 错误恢复（copy+remove 兜底）

这个函数有 **11 个职责**。根据单一职责原则，它应该被拆分为 11 个左右的小函数。但这不等于无脑拆分——关键是拆分后每个函数形成一个**抽象层级**。

### 5.3 改进方案：按抽象层级拆分

好的代码读起来像是在读概要，然后按需要深入细节：

```cpp
// 拆分后的 moveLogFile（顶层抽象，约 30 行）
bool FileMover::moveToArchive(const QString& sourceFile,
                               const QString& orderNumber,
                               const QString& barcode,
                               int& copyCount)
{
    if (!QFile::exists(sourceFile)) {
        m_logger->error(QString("源文件不存在: %1").arg(sourceFile));
        return false;
    }

    ensureOrderFolder(orderNumber);
    QString destFile = buildDestinationPath(orderNumber, sourceFile);

    // 检查并处理重复
    DuplicateInfo dupInfo = findDuplicate(destFile, orderNumber);
    if (dupInfo.exists) {
        if (!handleDuplicate(dupInfo, orderNumber, barcode, copyCount)) {
            return false;
        }
        // 如果旧文件在 completed 或 overlog，新文件也去那里
        destFile = buildDestinationPathForLocation(orderNumber, sourceFile, dupInfo.location);
    } else {
        // 检查溢出
        if (isOverflow(orderNumber)) {
            destFile = buildOverflowPath(orderNumber, sourceFile);
            m_alertService->notifyOverflow(orderNumber);
        }
    }

    return moveFile(sourceFile, destFile);
}

private:
// 下层抽象：每个函数只做一件事
void FileMover::ensureOrderFolder(const QString& orderNumber)
{
    QString path = m_config->buildArchivePath(orderNumber);
    if (!QDir(path).exists()) {
        createFolder(path);
    }
}

DuplicateInfo FileMover::findDuplicate(const QString& destFile, const QString& orderNumber)
{
    QStringList locations = {"根目录", "completed", "overlog"};
    QStringList subDirs  = {"",        "completed/", "overlog/"};

    for (int i = 0; i < locations.size(); ++i) {
        QString path = m_config->buildArchiveFilePath(orderNumber,
                         subDirs[i] + QFileInfo(destFile).fileName());
        if (QFile::exists(path)) {
            return {true, path, locations[i]};
        }
    }
    return {false, "", ""};
}

bool FileMover::handleDuplicate(const DuplicateInfo& dupInfo,
                                 const QString& orderNumber,
                                 const QString& barcode,
                                 int& copyCount)
{
    QString dupDir = m_config->buildDuplicateOrderPath(orderNumber);
    createFolder(dupDir);

    int seq = getNextSequence(barcode, orderNumber);
    copyCount = seq;
    QString newName = QString("%1+%2.log").arg(barcode).arg(seq);
    QString dupDest = QDir(dupDir).filePath(newName);

    return moveOrCopyAndRemove(dupInfo.path, dupDest);
}

bool FileMover::isOverflow(const QString& orderNumber)
{
    int current = m_orderManager->countValidFiles(orderNumber);
    int total   = m_orderManager->getTotal(orderNumber);
    return current >= total;
}
```

**关键技巧**：
- 顶层函数 `moveToArchive()` 读起来像一段自然语言：确保目录存在 → 找重复 → 处理重复或判断溢出 → 移动文件
- 每个 `private` 函数只做一件事，函数名就是做这件事的描述
- 如果你只关心"重复检测是怎么做的"，你只需要看 `findDuplicate()`，不用理会文件移动的细节

---

## 六、问题四：紧耦合与硬编码

### 6.1 什么是耦合（Coupling）

耦合描述的是**两个模块之间相互依赖的程度**。高耦合意味着改 A 必须改 B，低耦合意味着 A 和 B 可以独立变化。

### 6.2 你的代码中的耦合问题

#### 示例 1：硬编码的配置路径

`MainWindow::loadConfig()` （mainwindow.cpp:686）直接从 `"config.ini"` 读取：

```cpp
void MainWindow::loadConfig()
{
    if (!QFile::exists("config.ini")) {
        // ...
    }
    QSettings settings("config.ini", QSettings::IniFormat);
    // ...
}
```

这意味着：
- 单元测试无法给 MainWindow 注入测试用的配置值——必须真的有一个 config.ini 文件在程序运行目录
- 如果你需要支持多个配置文件（测试配置 vs 生产配置），必须改源码
- 路径写死为 `"config.ini"`，如果可执行文件不在你认为的目录，就会找不到

#### 示例 2：构造函数中执行复杂逻辑

`MainWindow::MainWindow()` 构造函数（mainwindow.cpp:145-263）内做了约 120 行操作，包括：

```cpp
MainWindow::MainWindow(QWidget *parent)
    : QMainWindow(parent)
{
    initLogger();
    // 执行 netuse.bat
    QProcess::execute(batPath);
    loadConfig();
    // 初始化文件监控
    fileWatcher = new QFileSystemWatcher(this);
    // 启动 5 个定时器
    processTimer = new QTimer(this);
    syncTimer = new QTimer(this);
    // ... 更多初始化 ...
    setupUI();
    setupWebSocket();
    setupAutoConnectTimer();
    // 延迟加载
    QTimer::singleShot(100, this, [this]() { askToLoadSavedWorkOrders(); });
    // 监控路径
    fileWatcher->addPath(watchPath);
    QTimer::singleShot(1000, this, &MainWindow::setupWorkOrderMonitoring);
}
```

构造函数中做了太多事导致两个问题：
- **不可测试**：创建 MainWindow 对象就意味着要执行 netuse.bat、读 config.ini、启动定时器——你无法"只测试 UI 部分"或"只测试文件监控部分"
- **启动脆弱**：构造函数中任何一个环节失败都可能导致整个程序崩溃

### 6.3 改进方案：依赖注入

#### 原则：不要找依赖，让依赖被给你

```cpp
// 之前（紧耦合）：MainWindow 自己从 config.ini 读配置
class MainWindow : public QMainWindow
{
    void loadConfig() {
        QSettings settings("config.ini", QSettings::IniFormat);
        watchPath = settings.value("Path/WatchFolder").toString();
        // ...
    }
};

// 之后（依赖注入）：配置从外部传入
class MainWindow : public QMainWindow
{
public:
    // 依赖显式声明在构造函数参数中
    explicit MainWindow(ConfigManager* config,
                        FileProcessor* fileProcessor,
                        OrderManager* orderManager,
                        WebSocketManager* wsManager,
                        QWidget* parent = nullptr);
};

MainWindow::MainWindow(ConfigManager* config,
                       FileProcessor* fileProcessor,
                       OrderManager* orderManager,
                       WebSocketManager* wsManager,
                       QWidget* parent)
    : QMainWindow(parent)
    , m_config(config)
    , m_fileProcessor(fileProcessor)
    , m_orderManager(orderManager)
    , m_wsManager(wsManager)
{
    // 构造函数现在只做最简单的 UI 初始化
    setupUI();
    connectSignals();
}
```

**测试时的好处**：

```cpp
// 测试中：可以注入假的配置
void TestMainWindow::testOverflowDisplay()
{
    // 创建一个纯内存的配置，不需要文件系统
    ConfigManager fakeConfig;
    fakeConfig.setArchivePath("/tmp/test_archive");
    fakeConfig.setMachineId("TEST");

    // 创建其他 fake/mock 对象
    FakeFileProcessor fakeProcessor;
    FakeOrderManager   fakeOrderManager;
    FakeWebSocketManager fakeWS;

    // 传入构造
    MainWindow window(&fakeConfig, &fakeProcessor, &fakeOrderManager, &fakeWS);

    // 测试：触发一次刷新，验证表格更新是否正确
    window.onRefresh();
    QCOMPARE(window.incompleteTableWidget->rowCount(), 3);
}
```

#### 构造函数应该只做初始化，不应该做"工作"

```cpp
// 不好的构造函数：自己做很多事
MainWindow::MainWindow() {
    initLogger();           // 副作用：创建日志文件
    loadConfig();           // 副作用：读文件
    startTimers();          // 副作用：定时器开始跑
    connectWebSocket();     // 副作用：尝试网络连接
}

// 好的构造函数：只保存依赖，把"开始工作"交给外部
MainWindow::MainWindow(deps...) : m_deps(deps) {
    setupUI();              // 纯 UI 初始化，无副作用
    connectSignals();        // 纯信号连接，无副作用
}

// 外部控制何时开始
void MainWindow::start() {
    m_fileWatcher->start();
    m_timers->start();
    // 明确标识"从这里开始，程序进入工作状态"
}

// main.cpp 中
MainWindow w(...);
w.show();
w.start();  // 显式启动业务逻辑
```

---

## 七、问题五：遗留死代码

### 7.1 你的代码中的死代码

**被整段注释的旧代码**（mainwindow.cpp:4199-4316，约 120 行）：

```cpp
/*void MainWindow::createActionButtons(int row)
{
    QWidget* widget = new QWidget();
    // ... 被注释的旧版按钮创建逻辑 ...
}*/
/*void MainWindow::setupUI()
{
    // ... 被注释的旧版 setupUI ...
}*/
```

**隐藏但未删除的旧表格**：

```cpp
// mainwindow.cpp:1178-1180
// 保留原有表格用于兼容性（隐藏）
tableWidget = new QTableWidget(this);
tableWidget->setVisible(false);  // 隐藏原有表格
```

这个 `tableWidget` 仍然存在于内存中，仍然被某些代码引用（如 `setupUI()` 中的右键菜单连接），但它已经不再显示。

**注释掉的日志语句**：

```cpp
// mainwindow.cpp:451
//LOG_INFO(QString("尝试添加文件到队列: %1").arg(filePath));
```

注释掉的调试日志是开发过程中的临时产物，不应该留在最终代码中。

### 7.2 为什么死代码有害

1. **混淆读者**：未来的维护者（包括 6 个月后的你自己）会困惑：这段代码是"暂时不用但以后会用"还是"彻底废弃了"？
2. **编译时间浪费**：注释掉的代码仍然需要被编译器解析（虽然不产生目标代码，但预处理器仍要处理）
3. **git 已有历史**：如果你用 git 管理代码，旧版本永远在 git 历史中，不需要在源码里保留副本

### 7.3 改进方案

```cpp
// 删除，而不是注释
// git 历史中永远可以找回旧版本：git log -p mainwindow.cpp

// 如果旧 tableWidget 确实不再需要
// 删除声明、删除初始化、删除所有引用

// 如果有某个功能需要"保留备用"，应该：
// 方案A：删掉，需要时从 git 历史恢复
// 方案B：如果真的需要，就保留它正常工作，而不是隐藏
```

**原则**：当你写了一段新代码替代旧代码时，确认新旧切换无误后，就**删除旧代码**。相信你的版本控制系统。

---

## 八、单元测试与集成测试详解

### 8.1 什么是测试，为什么要写

**测试是可自动运行、自动验证的代码**，用于确保你的程序行为符合预期。

核心价值不是"找出 bug"——而是**给你修改代码的信心**。当你重构了 500 行代码后，如果 100 个测试全部通过，你就知道没有引入回归 bug。

### 8.2 测试金字塔

```
        ┌──────┐
        │ E2E  │  ← 端到端测试：模拟完整操作流程，最慢、最贵
        │ 少量  │
       ┌┴──────┴┐
       │ 集成测试 │ ← 测试组件间协作，需要真实文件/数据库/网络
       │  适量   │
      ┌┴────────┴┐
      │  单元测试  │ ← 测试单个函数/类，毫秒级，不需要外部依赖
      │   大量    │
      └──────────┘
```

你的项目当前处于：**0 层测试**。每次改完代码，只能手动放文件到 watchPath 来验证。这是最不可靠的方式。

### 8.3 单元测试详解

#### 定义

单元测试验证**最小的功能单元**（通常是一个函数），在完全隔离的环境中运行。

- 不需要文件系统
- 不需要网络
- 不需要数据库
- 运行时间：毫秒级
- 一次跑几百个

#### 结合你的代码：测试 parseField5AsCopySizeMiB

这个函数位于 mainwindow.cpp:40-60 的匿名命名空间中：

```cpp
bool parseField5AsCopySizeMiB(const QString& field, qint64& out)
{
    QString s = field.trimmed();
    if (s.isEmpty())                    return false;
    if (s.contains(' ') || s.contains('\t')) return false;
    s.remove(',');
    if (s.isEmpty())                    return false;
    for (QChar c : s) {
        if (!c.isDigit())               return false;
    }
    bool ok = false;
    const qint64 v = s.toLongLong(&ok);
    if (!ok || v <= 0)                  return false;
    out = v;
    return true;
}
```

这是一个**纯逻辑函数**——给定一个字符串，返回一个结果。不依赖外部状态，不需要任何文件或网络。

#### 对应的单元测试（完整示例）

```cpp
// tests/tst_parsefield.cpp
#include <QTest>
#include <QString>

// 将函数声明从 mainwindow.cpp 的匿名命名空间中提取出来
// （建议将纯工具函数移到独立的 utils 文件中）
extern bool parseField5AsCopySizeMiB(const QString& field, qint64& out);

class TestParseField : public QObject
{
    Q_OBJECT

private slots:
    // 每个 slot 就是一个测试用例

    void normalInteger()
    {
        qint64 out = -1;
        QVERIFY(parseField5AsCopySizeMiB("1024", out));
        QCOMPARE(out, 1024);
    }

    void withCommaSeparator()
    {
        qint64 out = -1;
        // "1,024" 去掉逗号后应为 1024
        QVERIFY(parseField5AsCopySizeMiB("1,024", out));
        QCOMPARE(out, 1024);
    }

    void withMultipleCommas()
    {
        qint64 out = -1;
        QVERIFY(parseField5AsCopySizeMiB("1,234,567", out));
        QCOMPARE(out, 1234567);
    }

    void withLeadingTrailingSpaces()
    {
        qint64 out = -1;
        QVERIFY(parseField5AsCopySizeMiB("  2048  ", out));
        QCOMPARE(out, 2048);
    }

    void emptyString_shouldFail()
    {
        qint64 out = -1;
        QVERIFY(!parseField5AsCopySizeMiB("", out));
        // out 不应该被修改
        QCOMPARE(out, -1);
    }

    void zeroValue_shouldFail()
    {
        qint64 out = -1;
        QVERIFY(!parseField5AsCopySizeMiB("0", out));
    }

    void negativeValue_shouldFail()
    {
        qint64 out = -1;
        QVERIFY(!parseField5AsCopySizeMiB("-1", out));
        QVERIFY(!parseField5AsCopySizeMiB("-100", out));
    }

    void containsSpace_shouldFail()
    {
        qint64 out = -1;
        QVERIFY(!parseField5AsCopySizeMiB("12 34", out));
        QVERIFY(!parseField5AsCopySizeMiB("12\t34", out));  // 制表符
    }

    void nonDigitCharacters_shouldFail()
    {
        qint64 out = -1;
        QVERIFY(!parseField5AsCopySizeMiB("12.5", out));   // 小数点
        QVERIFY(!parseField5AsCopySizeMiB("abc", out));     // 字母
        QVERIFY(!parseField5AsCopySizeMiB("12a", out));     // 混合
        QVERIFY(!parseField5AsCopySizeMiB("0xFF", out));    // 十六进制
    }

    void largeNumber()
    {
        qint64 out = -1;
        // 测试极大值（约 9.2EB 的 MiB 值）
        QVERIFY(parseField5AsCopySizeMiB("9223372036854775807", out));
        QCOMPARE(out, 9223372036854775807LL);
    }

    void cleanup()
    {
        // Qt Test 在每个测试前后自动调用 init()/cleanup()
        // 这里不需要清理什么，因为测试不产生副作用
    }
};

// 这个宏生成 main 函数，自动执行所有测试
QTEST_APPLESS_MAIN(TestParseField)
```

运行结果会是：
```
********* Start testing of TestParseField *********
Config: Using QtTest library 6.x.x
PASS   : TestParseField::normalInteger()
PASS   : TestParseField::withCommaSeparator()
PASS   : TestParseField::withMultipleCommas()
PASS   : TestParseField::withLeadingTrailingSpaces()
PASS   : TestParseField::emptyString_shouldFail()
PASS   : TestParseField::zeroValue_shouldFail()
PASS   : TestParseField::negativeValue_shouldFail()
PASS   : TestParseField::containsSpace_shouldFail()
PASS   : TestParseField::nonDigitCharacters_shouldFail()
PASS   : TestParseField::largeNumber()
Totals: 10 passed, 0 failed, 0 skipped
********* Finished testing of TestParseField *********
```

10 个测试用例，执行时间 < 1 毫秒。

#### 另一个单元测试示例：测试 validateBarcode

```cpp
// tests/tst_validatebarcode.cpp
#include <QTest>

// 假设已经将 validateBarcode 提取到 BarcodeValidator 类中
#include "barcodevalidator.h"

class TestBarcodeValidator : public QObject
{
    Q_OBJECT

private slots:
    void matchingBarcode_shouldPass()
    {
        BarcodeValidator validator;
        // 旧格式：工单号|条码
        QString filePath = "D:/Watch/ABC123.log";
        QString content  = "WO001|ABC123";

        QVERIFY(validator.validate(filePath, content));
    }

    void mismatchedBarcode_shouldFail()
    {
        BarcodeValidator validator;
        QString filePath = "D:/Watch/ABC123.log";
        QString content  = "WO001|XYZ789";

        QVERIFY(!validator.validate(filePath, content));
    }

    void newFormatWithMaterialCode()
    {
        BarcodeValidator validator;
        // 新格式：工单号|条码|料号描述
        QString filePath = "D:/Watch/BAR456.log";
        QString content  = "WO002|BAR456|SAMSUNG_K9OKGY8S7M";

        QVERIFY(validator.validate(filePath, content));
    }

    void insufficientFields_shouldFail()
    {
        BarcodeValidator validator;
        QString filePath = "D:/Watch/TEST.log";
        QString content  = "WO001";  // 只有工单号，没有条码

        QVERIFY(!validator.validate(filePath, content));
    }

    void emptyContent_shouldFail()
    {
        BarcodeValidator validator;
        QString filePath = "D:/Watch/TEST.log";
        QString content  = "";

        QVERIFY(!validator.validate(filePath, content));
    }

    void filenameWithoutExtension()
    {
        BarcodeValidator validator;
        // 文件名没有扩展名的情况（baseName 仍然正确）
        QString filePath = "D:/Watch/TEST001";
        QString content  = "WO003|TEST001";

        QVERIFY(validator.validate(filePath, content));
    }
};

QTEST_APPLESS_MAIN(TestBarcodeValidator)
```

### 8.4 集成测试详解

#### 与单元测试的区别

| | 单元测试 | 集成测试 |
|------|---------|---------|
| 测试对象 | 单个函数/方法 | 多个组件协作 |
| 外部依赖 | 不需要 | 需要文件系统/测试服务器 |
| 运行速度 | 毫秒级 | 毫秒到秒级 |
| 失败时定位 | 直接定位到具体函数 | 需要排查是哪个环节的问题 |
| 覆盖的场景 | 边界值、异常输入 | 完整业务流程 |

#### 结合你的代码：集成测试 processLogFile 全流程

```cpp
// tests/tst_processlogfile.cpp
#include <QTest>
#include <QTemporaryDir>
#include <QFile>
#include <QDir>

// 假设已经将核心类拆分
#include "fileprocessor.h"
#include "configmanager.h"
#include "materialvalidator.h"
#include "filemover.h"
#include "timerecordwriter.h"

class TestFileProcessing : public QObject
{
    Q_OBJECT

private:
    // 测试环境：用临时目录模拟生产路径
    QTemporaryDir m_watchDir;        // 模拟 watchPath
    QTemporaryDir m_archiveDir;      // 模拟 archivePath
    QTemporaryDir m_duplicateDir;    // 模拟 duplicatePath
    QTemporaryDir m_workordersRoot;  // 模拟 workordersRootPath

    ConfigManager*     m_config;
    OrderManager*      m_orderManager;
    MaterialValidator* m_materialValidator;
    AlertService*      m_alertService;
    FileProcessor*     m_fileProcessor;

    // 在每个测试用例开始前执行
    void init() override
    {
        // 构造配置对象，注入临时路径
        m_config = new ConfigManager(this);
        m_config->setWatchPath(m_watchDir.path());
        m_config->setArchivePath(m_archiveDir.path());
        m_config->setDuplicatePath(m_duplicateDir.path());
        m_config->setWorkordersRoot(m_workordersRoot.path());
        m_config->setMachineId("TEST_MACHINE");

        // 创建其他业务对象
        m_orderManager      = new OrderManager(m_config, this);
        m_alertService      = new AlertService(this);
        m_materialValidator = new MaterialValidator(m_config, this);
        m_fileProcessor     = new FileProcessor(
                                  m_materialValidator,
                                  new FileMover(m_config, m_alertService, this),
                                  new TimeRecordWriter(m_config, this),
                                  this
                              );
    }

private slots:
    void normalFileProcessing()
    {
        // 准备：创建工作单和 NUM.txt
        QDir(m_workordersRoot.path()).mkpath("WO_TEST");
        QFile numFile(m_workordersRoot.path() + "/WO_TEST/NUM.txt");
        numFile.open(QIODevice::WriteOnly | QIODevice::Text);
        numFile.write("100");
        numFile.close();

        // 加载工单
        m_orderManager->addOrder("WO_TEST", 100);

        // 准备：在 watchPath 创建测试文件
        // 旧格式：工单号|条码
        QString testFilePath = m_watchDir.path() + "/TEST001.log";
        QFile testFile(testFilePath);
        testFile.open(QIODevice::WriteOnly | QIODevice::Text);
        testFile.write("WO_TEST|TEST001");
        testFile.close();

        // 准备：料号白名单初始化为空（首次处理自动加入）
        // MaterialValidator 会自动处理

        // 执行
        bool result = m_fileProcessor->process(testFilePath);

        // 验证
        QVERIFY(result);

        // 文件应该从 watchPath 移到 archivePath
        QVERIFY(!QFile::exists(testFilePath));
        QVERIFY(QFile::exists(m_archiveDir.path() + "/WO_TEST/TEST001.log"));

        // 工单计数应该更新
        QCOMPARE(m_orderManager->getOrder("WO_TEST")->completedCount, 1);

        // TimeCount 文件应该被创建
        QVERIFY(QFile::exists(m_archiveDir.path() +
                              "/WO_TEST/TimeCountTEST_MACHINE.txt"));
    }

    void overflowScenario()
    {
        // 准备：总数设为 1
        QDir(m_workordersRoot.path()).mkpath("WO_OVERFLOW");
        QFile numFile(m_workordersRoot.path() + "/WO_OVERFLOW/NUM.txt");
        numFile.open(QIODevice::WriteOnly | QIODevice::Text);
        numFile.write("1");
        numFile.close();

        m_orderManager->addOrder("WO_OVERFLOW", 1);

        // 模拟：已有一个文件在归档中（计数 = 1，已达到总数）
        QDir(m_archiveDir.path()).mkpath("WO_OVERFLOW/completed");
        QFile existing(m_archiveDir.path() + "/WO_OVERFLOW/completed/EXISTING.log");
        existing.open(QIODevice::WriteOnly);
        existing.write("dummy");
        existing.close();

        // 更新工单计数以反映已有文件
        m_orderManager->getOrder("WO_OVERFLOW")->completedCount = 1;

        // 准备新文件
        QString testFilePath = m_watchDir.path() + "/OVERFLOW_TEST.log";
        QFile testFile(testFilePath);
        testFile.open(QIODevice::WriteOnly | QIODevice::Text);
        testFile.write("WO_OVERFLOW|OVERFLOW_TEST");
        testFile.close();

        // 执行
        bool result = m_fileProcessor->process(testFilePath);

        // 验证
        QVERIFY(result);

        // 文件应该进入 overlog 而不是根目录
        QVERIFY(!QFile::exists(m_archiveDir.path() + "/WO_OVERFLOW/OVERFLOW_TEST.log"));
        QVERIFY(QFile::exists(m_archiveDir.path() + "/WO_OVERFLOW/overlog/OVERFLOW_TEST.log"));
    }

    void duplicateFileHandling()
    {
        // 准备
        QDir(m_workordersRoot.path()).mkpath("WO_DUP");
        QFile numFile(m_workordersRoot.path() + "/WO_DUP/NUM.txt");
        numFile.open(QIODevice::WriteOnly | QIODevice::Text);
        numFile.write("10");
        numFile.close();

        m_orderManager->addOrder("WO_DUP", 10);

        // 在归档目录放一个同名文件（模拟重复）
        QDir(m_archiveDir.path()).mkpath("WO_DUP");
        QFile existing(m_archiveDir.path() + "/WO_DUP/DUP_TEST.log");
        existing.open(QIODevice::WriteOnly | QIODevice::Text);
        existing.write("WO_DUP|DUP_TEST");
        existing.close();

        // 新文件（同名）
        QString testFilePath = m_watchDir.path() + "/DUP_TEST.log";
        QFile testFile(testFilePath);
        testFile.open(QIODevice::WriteOnly | QIODevice::Text);
        testFile.write("WO_DUP|DUP_TEST");
        testFile.close();

        // 执行
        bool result = m_fileProcessor->process(testFilePath);

        // 验证
        QVERIFY(result);

        // 旧文件被移到重复目录，命名为 条码+1.log
        QVERIFY(QFile::exists(m_duplicateDir.path() + "/WO_DUP/DUP_TEST+1.log"));

        // 新文件被放到归档根目录
        QVERIFY(QFile::exists(m_archiveDir.path() + "/WO_DUP/DUP_TEST.log"));
    }

    void cleanup() override
    {
        // QTemporaryDir 在析构时自动清理
        // 不需要手动删除
    }
};

QTEST_MAIN(TestFileProcessing)
// 用 QTEST_MAIN 而不是 QTEST_APPLESS_MAIN
// 因为集成测试可能需要 QApplication 事件循环
```

### 8.5 如何组织测试项目

```
copyssd/
├── CMakeLists.txt              # 主 CMake，加几行启用测试
├── src/                        # 源代码（建议从根目录移到 src/）
│   ├── main.cpp
│   ├── mainwindow.h
│   ├── mainwindow.cpp
│   ├── fileprocessor.h         # 拆分后新增
│   ├── fileprocessor.cpp
│   ├── configmanager.h
│   ├── configmanager.cpp
│   ├── ...
│   └── utils/
│       ├── copylog_utils.h     # 工具函数（如 parseField5）
│       └── copylog_utils.cpp
└── tests/                      # 测试代码
    ├── CMakeLists.txt
    ├── tst_main.cpp            # 测试入口
    ├── tst_parsefield.cpp      # 单元测试
    ├── tst_validatebarcode.cpp # 单元测试
    └── tst_processlogfile.cpp  # 集成测试
```

**tests/CMakeLists.txt**：
```cmake
find_package(Qt${QT_VERSION_MAJOR} REQUIRED COMPONENTS Test)

# 测试可执行文件
add_executable(copyssd_test
    tst_main.cpp
    tst_parsefield.cpp
    tst_validatebarcode.cpp
    tst_processlogfile.cpp
    # 链接被测源码
    ${CMAKE_SOURCE_DIR}/src/configmanager.cpp
    ${CMAKE_SOURCE_DIR}/src/fileprocessor.cpp
    ${CMAKE_SOURCE_DIR}/src/materialvalidator.cpp
    ${CMAKE_SOURCE_DIR}/src/utils/copylog_utils.cpp
    # ... 其他被测文件 ...
)

target_link_libraries(copyssd_test PRIVATE
    Qt${QT_VERSION_MAJOR}::Widgets
    Qt${QT_VERSION_MAJOR}::WebSockets
    Qt${QT_VERSION_MAJOR}::Concurrent
    Qt${QT_VERSION_MAJOR}::Test
)

target_include_directories(copyssd_test PRIVATE ${CMAKE_SOURCE_DIR}/src)

# 注册到 CTest
add_test(NAME copyssd_unit_tests COMMAND copyssd_test)
```

**主的 CMakeLists.txt 末尾加上**：
```cmake
enable_testing()
add_subdirectory(tests)
```

然后就可以：
```bash
# 编译并运行所有测试
cmake --build . --target copyssd_test
ctest --verbose

# 或者直接运行测试可执行文件看详细结果
./copyssd_test
```

### 8.6 测试的常见疑问

**Q: 测试要写多少才够？**

至少覆盖：
- 纯逻辑函数（如 parseField、validateBarcode）——全部边界情况
- 核心业务流程（processLogFile 的正常、溢出、重复三条路径）
- 曾经出过 bug 的场景（修复后立即补测试，防止再次崩坏）

不要求 100% 覆盖率，但核心路径必须有。

**Q: 测试代码会不会增加维护负担？**

短期看起来多写了代码。但长期效果相反：你花在手工验证、排查回归 bug 上的时间会大幅减少。测试是**投资**，不是成本。

**Q: 我现在怎么开始？**

从最简单的开始——给 `parseField5AsCopySizeMiB` 写 10 个单元测试。这是唯一的纯逻辑函数，不需要任何重构就能被测试。跑通这个流程后，再逐步拆分 MainWindow 并添加更多测试。

---

## 九、设计原则清单

以下是软件开发中最常用、最实用的设计原则。每一条都用你的代码场景做了解释。

### SOLID 五原则

| 原则 | 全称 | 你的问题 | 改进方向 |
|------|------|----------|----------|
| **S** | 单一职责原则 | MainWindow 做 20+ 件事 | 拆分为 FileProcessor、OrderManager 等独立类 |
| **O** | 开闭原则 | 加新报警类型要改 FullScreenAlertDialog | 报警类型应可扩展（继承/策略模式），不修改已有代码 |
| **I** | 接口隔离原则 | 不相关（你的代码没定义接口） | 如果定义接口，不要让调用方依赖它不需要的方法 |
| **L** | 里氏替换原则 | 不相关（你的代码没用到继承） | 子类应该能完全替代父类 |
| **D** | 依赖倒置原则 | MainWindow 直接读 config.ini | 通过 ConfigManager 注入配置，MainWindow 不关心配置从哪里来 |

### 其他关键原则

| 原则 | 含义 | 你的问题 |
|------|------|----------|
| **DRY** | 不要重复自己 | 网络路径构造复制了 15 次 |
| **KISS** | 保持简单 | 有些方法的 if-else 嵌套过深 |
| **YAGNI** | 你不会需要它 | 注释掉的代码以"可能以后会用到"的理由保留 |
| **童子军规则** | 离开时让营地比来时更干净 | 每次加新功能时顺手做点小重构 |
| **最少知识原则** | 一个对象应该对其它对象了解得尽可能少 | MainWindow 了解文件移动的所有细节 |

### 代码坏味道 (Code Smells)

这些是需要警惕的信号：

| 坏味道 | 表现 | 你的代码中 |
|--------|------|-----------|
| 过长方法 | 一个方法超过 30-50 行 | `moveLogFile` 320 行, `onRefresh` 200 行 |
| 过大的类 | 一个类超过 300-500 行 | `MainWindow` 5600+ 行 |
| 过多参数 | 函数参数超过 3-4 个 | `handleErrorFileWithOrder` 4 个参数（可接受） |
| 霰弹式修改 | 改一个功能需要改 10+ 个地方 | 改路径逻辑要改 15 个函数 |
| 特性依恋 | 一个方法频繁访问另一个对象的数据 | `MainWindow` 的方法大量直接操作 `WorkOrder` 内部字段 |
| 注释掉的代码 | 废弃的逻辑留在源码中 | mainwindow.cpp:4199-4316 约 120 行 |

---

## 十、渐进式重构路线图

重构不是重写。不要试图一次性把所有问题都修复——那样风险太大。正确的做法是小步快跑，每次改一点点，改完验证，提交，再继续。

### 第一阶段：建立安全网（优先级最高）

**目标**：让你敢改代码。

1. 把 `parseField5AsCopySizeMiB` 移到独立文件 `src/utils/copylog_utils.h/cpp`
2. 给这个函数写 10 个单元测试
3. 搭建 CMake 测试框架（`enable_testing`, `add_subdirectory(tests)`）
4. 确保 `ctest` 能跑

这一步不会改变任何业务逻辑，但建立了**修改代码的安全感**——之后每次改完代码，跑一遍测试，通过了就说明没引入新问题。

### 第二阶段：消除重复（风险低，收益高）

1. 在 `MainWindow` 中添加私有辅助方法 `buildArchivePath(orderNumber)`, `joinPath(base, sub)` 等
2. 逐个修改 15 个函数，用辅助方法替换重复的路径构造代码
3. 每改一个，编译并通过手工测试验证
4. 全部替换后，删除旧的重复代码

### 第三阶段：拆分核心业务类（中等风险）

按本文第三章的方案，逐步拆出：

1. `ConfigManager` → 从 `loadConfig` 独立出来，让 MainWindow 接受注入
2. `FileMover` → 从 `moveLogFile` 及其相关方法独立
3. `MaterialValidator` → 从 `validateMaterialCode` 及相关方法独立
4. `FileProcessor` → 从 `processLogFile` 独立，依赖上面已拆出的类

每拆出一个类，就给它的关键方法写测试。

### 第四阶段：优化 UI 分离（低优先级）

1. 把通知/报警逻辑从 MainWindow 移到 `AlertService`
2. 把 WebSocket 逻辑移到 `WebSocketManager`
3. 让 MainWindow 只做 UI 组装和信号路由

### 什么不应该重构

- **能正常运行的生产代码**：如果它没有被修改的需求，就不需要为了"完美"而重构
- **不熟悉的代码**：先理解，再动手
- **没有测试保护的代码**：先加上测试（第一阶段），再重构

---

## 结语

你的第一个项目能做到这个程度——覆盖完整的产线业务、处理网络路径兼容、防并发冲突、防 UI 死锁——说明你的工程直觉是好的。你现在缺少的不是"怎么让程序跑起来"的能力，而是"怎么让代码容易被修改和维护"的知识。

这些知识大部分不能从实战中自然获得——因为产线环境衡量的是"程序能不能用"，不是"代码好不好看"。你需要在实战之外系统性地学习软件设计。

建议后续阅读：
- 《重构：改善既有代码的设计》Martin Fowler — 你会在书中看到自己的影子
- 《代码整洁之道》Robert C. Martin — SOLID 原则和代码坏味道的经典
- 《单元测试的艺术》Roy Osherove — 帮助你理解测试的实践
