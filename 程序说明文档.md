# copyssd 程序说明文档

## 一、项目概述

**copyssd** 是一个基于 Qt (C++) 的 Windows 桌面应用程序，用于 SSD 拷贝生产线的工单管理与文件自动归档。程序通过监控指定文件夹中的日志文件，自动解析工单号与条码信息，完成文件的分类归档、重复检测、溢出报警，并通过 WebSocket 向中控服务器上报工单状态，同时支持通过 Windows 消息机制与厂商拷贝程序交互。

- **开发框架**: Qt 6.10.1 (兼容 Qt 5)
- **构建系统**: CMake
- **编程语言**: C++17
- **运行平台**: Windows
- **版本**: 0.1

---

## 二、项目文件结构

```
copyssd/
├── CMakeLists.txt              # CMake 构建配置
├── main.cpp                    # 应用程序入口
├── mainwindow.h                # 主窗口头文件（含 OverflowAlertManager 单例类）
├── mainwindow.cpp              # 主窗口实现（核心业务逻辑，约5600行）
├── mainwindow.ui               # Qt Designer 界面文件（基础布局骨架）
├── mainwindow_original_backup.h # 原始版本备份文件
├── workorder.h                 # 工单数据模型类
├── logger.h / logger.cpp       # 日志系统（单例模式，支持日志轮转）
├── websocketclient.h / .cpp    # WebSocket 客户端（连接中控服务器）
├── copycontroldialog.h / .cpp  # 拷贝控制对话框（Windows 消息控制厂商程序）
├── fullscreenalertdialog.h / .cpp # 全屏报警对话框（溢出/异常/料号确认）
├── workorderselectdialog.h / .cpp # 工单选择加载对话框
├── MessageControl.cpp          # MFC 版消息控制应用（遗留代码，非 Qt 项目部分）
├── MessageControlDlg.cpp       # MFC 版消息控制对话框（遗留代码）
├── resources.qrc               # Qt 资源文件（应用图标）
├── config.ini                  # 配置文件（需自行创建或由程序自动生成默认配置）
├── saved_workorders.json       # 工单持久化文件（程序退出时保存）
└── netuse.bat                  # 网络映射脚本（启动时自动执行，可选）
```

> **注意**: `MessageControl.cpp` 和 `MessageControlDlg.cpp` 是 MFC（Microsoft Foundation Classes）框架下的遗留代码，不属于当前 Qt 项目。它们实现了与 `CopyControlDialog` 相同的功能（通过 Windows 消息控制拷贝机），但使用 MFC 对话框和 `FindWindow("SataCopyApp")` 查找厂商窗口。此部分代码由 CMakeLists.txt 外独立构建，本文档不再详述。

---

## 三、核心类与模块设计

### 3.1 类关系总览

```
MainWindow (QMainWindow)
├── WorkOrder (数据模型)
├── WebSocketClient (网络通信)
├── QFileSystemWatcher (文件监控)
├── QQueue<QString> (文件处理队列)
├── QTimer (定时器：处理、同步、清理、中控自动连接)
├── OverflowAlertManager (单例，溢出报警管理)
│   └── FullScreenAlertDialog (全屏报警窗口)
├── Logger (单例，日志记录)
├── CopyControlDialog (拷贝控制弹窗，按需创建)
└── WorkOrderSelectDialog (工单选择弹窗，按需创建)
```

### 3.2 WorkOrder（workorder.h）

工单数据模型，纯数据类。

| 字段 | 类型 | 说明 |
|------|------|------|
| `orderNumber` | QString | 工单号 |
| `totalCount` | int | 总数量（从 NUM.txt 读取） |
| `completedCount` | int | 已完成数量（根据文件夹内 log 文件数统计） |
| `failedCount` | int | 失败数量（人工标记） |
| `isCountingStopped` | bool | 是否停止计数 |

关键方法: `getRemainingCount()` — 返回 `totalCount - completedCount - failedCount`。

### 3.3 Logger（logger.h / logger.cpp）

单例模式的日志系统，线程安全。

- **`getInstance()`**: 获取单例实例（静态局部变量，线程安全）
- **`log(LogLevel, message)`**: 写入日志，带时间戳和级别标记
- **`setLogFile(path)`**: 设置日志文件路径
- **`checkLogFileSize()` / `rotateLogFiles()`**: 日志轮转，单文件超过 10MB 自动轮转，最多保留 5 个备份

日志级别枚举: `Debug`, `Info`, `Warning`, `Error`

便捷宏:
```cpp
#define LOG_DEBUG(msg)    Logger::getInstance().log(Logger::Debug, msg)
#define LOG_INFO(msg)     Logger::getInstance().log(Logger::Info, msg)
#define LOG_WARNING(msg)  Logger::getInstance().log(Logger::Warning, msg)
#define LOG_ERROR(msg)    Logger::getInstance().log(Logger::Error, msg)
```

### 3.4 WebSocketClient（websocketclient.h / websocketclient.cpp）

与中控服务器的 WebSocket 通信客户端。

- **`connectToServer()`**: 连接到 `ws://<IP>:<Port>`（地址从 `config.ini` → `[CentralServer]` 读取）
- **`disconnectFromServer()`**: 断开连接
- **`sendWorkOrdersInfo(QJsonObject)`**: 发送工单信息 JSON 到服务器
- **`isConnected()` / `getConnectionStatus()`**: 连接状态查询

**信号**:
- `connectionStatusChanged(status)` — 连接状态文本变化
- `connected()` — 连接成功
- `disconnected()` — 连接断开
- `error(errorString)` — 连接错误

**依赖配置项**（`config.ini`）:
| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| `CentralServer/IP` | localhost | 中控服务器 IP |
| `CentralServer/Port` | 8080 | 中控服务器端口 |

### 3.5 CopyControlDialog（copycontroldialog.h / copycontroldialog.cpp）

通过 Windows 消息机制控制厂商拷贝程序（SataCopyApp）的对话框。

**调用关系**: `MainWindow::onStartCopyClicked()` → 创建 `CopyControlDialog` 并以 `exec()` 模态运行。

**核心功能**:
- **发送工单号**: 写入 `E:/OrderNumber.txt` → `PostMessage(WM_COPY_ORDER_NUMBER)`
- **开始/停止拷贝**: `PostMessage(WM_COPY_START)` / `PostMessage(WM_COPY_STOP)`
- **设置工作模式**: `PostMessage(WM_COPY_WORK_MODE, 0, mode)` — 支持6种模式（快速拷贝、快速拷贝+比对、全盘拷贝、全盘拷贝+比对、制作镜像、镜像拷贝）

**厂商窗口查找策略**（`findVendorWindow()`）:
1. 使用配置的窗口类名 `Vendor/WindowClass`
2. 使用配置的窗口标题 `Vendor/WindowTitle`
3. 尝试常见类名列表 (`SataCopyApp`, `SataCopy`, `CopyApp` 等)
4. 枚举所有窗口，按标题部分匹配 (`SataCopy`, `Copy`, `拷贝`, `复制`)

**依赖配置项**（`config.ini`）:
| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| `Vendor/OrderNumberFile` | E:/OrderNumber.txt | 工单号写入文件路径 |
| `Vendor/WindowClass` | 空 | 厂商窗口类名 |
| `Vendor/WindowTitle` | 空 | 厂商窗口标题 |

### 3.6 FullScreenAlertDialog（fullscreenalertdialog.h / fullscreenalertdialog.cpp）

全屏无边框报警/确认对话框。支持四种类型:

| 类型 | 用途 | 模态 | 按钮 |
|------|------|------|------|
| `OverflowAlert` | 工单溢出报警 | 是（ApplicationModal） | 确认 + 关闭 |
| `AnomalyAlert` | 工单状态异常报警 | 否 | 确认 + 关闭 |
| `MaterialConfirm` | 料号确认报警 | 是（ApplicationModal） | 确认添加 + 拒绝料号 |
| `FileOperationError` | NUM.txt 文件操作失败 | 否 | 确认 + 关闭 |

**核心机制**:
- **全屏显示**: 覆盖整个主屏幕，半透明黑色背景（`rgba(0,0,0,0.7)`）
- **多重激活**: 在构造、`showEvent`、以及延迟定时器（0ms/50ms/200ms）中分别尝试 `raise()` + `activateWindow()` + `SetForegroundWindow()`（Windows API），三重保险确保窗口获得焦点
- **ESC 键兜底机制**: 安装全局事件过滤器（`QApplication::installEventFilter`），通过静态列表 `s_activeDialogs` 追踪所有活动窗口，即使窗口失去焦点也能响应 ESC 键强制关闭；对于 `MaterialConfirm` 类型，ESC 等同于点击"拒绝料号"按钮
- **信号**: `confirmed()`, `rejected()`, `closed()`
- **回调支持**: `setCallback(std::function<void(bool)>)` — 用于外部响应确认/拒绝操作

### 3.7 OverflowAlertManager（mainwindow.h 内部类，单例模式）

工单溢出报警的集中管理器。

- **`getInstance()`**: 获取单例实例，首次调用时初始化冷却定时器（60秒间隔）
- **`showOverflowAlert(orderNumber)`**: 显示溢出报警（带10分钟冷却机制，10分钟内同一工单不重复弹窗）
- **`closeAlert()`**: 关闭报警窗口，重置所有工单冷却时间
- **`onOverflowResolved(orderNumber)`**: 标记溢出已解决，清除该工单冷却时间
- **`checkCooldownExpired()`**: 由定时器每分钟调用，检查冷却时间过期状态

### 3.8 WorkOrderSelectDialog（workorderselectdialog.h / workorderselectdialog.cpp）

启动时加载已保存工单的选择对话框。显示每个工单的工单号、总数、已完成数、剩余数，支持勾选/全选/取消全选。

---

## 四、主要功能与实现

### 4.1 程序启动流程

**入口函数**: `main()` → `mainwindow.cpp:1-12`

```
main()
├── 创建 QApplication，设置窗口图标 (ldb.ico)
├── 创建 MainWindow 实例
│   ├── initLogger()                          → 初始化日志系统
│   ├── 执行 netuse.bat (如果存在)             → 建立网络映射
│   ├── loadConfig()                          → 加载 config.ini
│   ├── 初始化 QFileSystemWatcher (文件监控)
│   ├── 启动 processTimer (100ms 间隔)        → 驱动文件处理队列
│   ├── 启动 syncTimer (30s 间隔)             → 定期同步工单计数
│   ├── 启动 queueProgressTimer (2s 间隔)     → 更新队列进度
│   ├── 启动 notificationCleanupTimer (24h)   → 清理过期通知记录
│   ├── 启动 pendingOrderTimer (5s 间隔)      → 处理等待确认的工单
│   ├── setupUI()                             → 构建双表格界面
│   ├── setupWebSocket()                      → 初始化 WebSocket 连接
│   ├── setupAutoConnectTimer()               → 设置中控自动重连定时器
│   ├── askToLoadSavedWorkOrders()            → 询问用户加载上次保存的工单
│   ├── 添加 watchPath 到文件监控
│   ├── 延迟 1s → setupWorkOrderMonitoring()  → 设置所有工单文件夹监控
│   └── 确保 archivePath 存在
└── w.show() → a.exec()
```

### 4.2 配置加载

**实现函数**: `MainWindow::loadConfig()` → `mainwindow.cpp:686`
**默认配置生成**: `MainWindow::createDefaultConfig()` → `mainwindow.cpp:839`

从 `config.ini`（IniFormat）读取所有配置项，验证路径存在性，不存在则尝试创建。网络路径（`\\` 或 `//` 开头）不尝试创建，仅检查可访问性。

**全部配置项依赖**（`config.ini`）:

| Section/Key | 默认值 | 用途 |
|-------------|--------|------|
| `Machine/ID` | DEFAULT | 机器标识，用于 TimeCount 文件名 |
| `Path/WatchFolder` | D:/Watch | 监控文件夹（log 文件来源） |
| `Path/ArchiveFolder` | D:/Archive | 归档根目录（按工单号建子文件夹） |
| `Path/DuplicateFolder` | D:/Duplicate | 重复文件存放路径 |
| `Path/MismatchFolder` | D:/Mismatch | 条码不匹配文件存放路径 |
| `Path/ErrorFolder` | D:/Error | 错误文件存放路径 |
| `Path/MaterialCachePath` | E:/MaterialCache | 料号确认缓存路径 |
| `Path/WorkordersRoot` | 空 | 工单根目录（每个工单文件夹内含 NUM.txt） |
| `Path/WorkorderConfig` | D:/Config/workorder.ini | 工单配置文件路径（旧方式） |
| `Path/GobRoute1` | \\172.18.10.246\COPYSDD | 源文件服务器路径1（大小校验用） |
| `Path/GobRoute2` | \\172.18.10.246\COPYSDD | 源文件服务器路径2（备选） |
| `Path/CopyMachineLogPath` | D:/CopyLogs/ | 拷贝机记录路径（大小校验用） |
| `Settings/SyncValidationInterval` | 20 | 大小校验触发间隔（syncTimer 周期倍数） |
| `CentralServer/IP` | localhost | 中控服务器 IP |
| `CentralServer/Port` | 8080 | 中控服务器端口 |
| `SSDInfo/SSDInfoPath` | E:/SSDInfo | SSD 信息文件路径（旧料号方案） |
| `Vendor/OrderNumberFile` | E:/OrderNumber.txt | 拷贝控制工单号写入路径（可选） |
| `Vendor/WindowClass` | 空 | 拷贝控制厂商窗口类名（可选） |
| `Vendor/WindowTitle` | 空 | 拷贝控制厂商窗口标题（可选） |

### 4.3 文件监控与队列处理

#### 4.3.1 文件监控架构

```
QFileSystemWatcher (fileWatcher)
├── watchPath                      → onDirectoryChanged()
│   └── processDirectoryFiles()    → 处理监控目录中的新文件
├── archivePath/<orderNumber>/     → onArchiveDirectoryChanged()
│   └── updateWorkOrderCountFromPath()
├── archivePath/<orderNumber>/completed/
└── archivePath/<orderNumber>/overlog/
```

**监控设置**: `setupWorkOrderMonitoring()` → `mainwindow.cpp:2853`

为每个已加载工单在 `archivePath` 下的根目录、`completed/` 子目录、`overlog/` 子目录分别添加文件监控。

#### 4.3.2 队列处理机制

**核心数据结构**: `QQueue<QString> fileQueue` — 文件路径队列
**驱动定时器**: `processTimer`（100ms 间隔）
**处理函数**: `processNextFile()` → `mainwindow.cpp:405`
**入队函数**: `addToProcessQueue()` → `mainwindow.cpp:449`

处理流程:
```
文件到达 watchPath
    ↓
onDirectoryChanged() → 延迟1s → processDirectoryFiles()
    ↓
ensureLogExtension() (txt→log 重命名)
    ↓
addToProcessQueue() (防重复入队)
    ↓
processTimer(100ms) → processNextFile()
    ↓
processLogFile() (核心处理逻辑)
```

**批量优化**（`processAllFilesOptimized()` → `mainwindow.cpp:946`）:
- 文件数 < 100: 使用进度对话框逐个添加
- 文件数 ≥ 100: 按每批 50 个分批次入队，每批次间延迟 10ms，避免界面卡死

### 4.4 日志文件处理（核心业务流程）

**实现函数**: `processLogFile()` → `mainwindow.cpp:2935`

这是程序最核心的业务逻辑函数，完整的文件处理流程：

```
processLogFile(filePath)
│
├── 1. 检查文件名是否有效 (baseName 非空)
│
├── 2. 读取文件内容（带重试机制，最多10次，间隔500ms）
│     处理文件占用错误（ResourceError / "另一个程序正在使用此文件"）
│
├── 3. validateBarcode()
│     解析文件内容: 工单号|条码|料号描述 (或旧格式: 工单号|条码)
│     比对文件名(baseName) 与 内容中条码 是否一致
│     不匹配 → handleMismatchedFile() 移动到 mismatchPath
│
├── 4. validateMaterialCode()
│     ├── 工单在白名单? → 通过
│     ├── 工单在黑名单? → 拒绝，handleErrorFileWithOrder()
│     ├── 首次出现新料号 → showMaterialCodeConfirmationDialog()
│     │   + addToPendingOrderQueue() + moveFileToMaterialCache()
│     └── 等待确认中 → 跳过处理
│
├── 5. recordProcessedOrder() + syncProcessedOrders()
│
├── 6. 获取文件创建时间 (birthTime)
│
├── 7. moveLogFile()
│     ├── checkAndCreateOrderFolder()  确保工单文件夹存在
│     ├── 检查目标位置是否已有同名文件
│     │   ├── 根目录 / completed / overlog 三处检查
│     │   └── 有重复 → 旧文件移到 duplicatePath，rename+cop
│     │               新文件根据旧文件位置确定目标子目录
│     ├── 检查是否溢出 (countAllLogFilesInOrderFolder >= totalCount)
│     │   └── 溢出 → 目标改为 overlog/，触发 showOverflowAlert()
│     └── QFile::rename() 移动文件
│
├── 8. updateWorkOrderCount()  更新内存中的工单计数
│
└── 9. writeTimeRecord()  写入 TimeCount<MachineID>.txt
       格式: 创建时间|工单号|条码|出现次数|工单总数|已完成数量|剩余量|当前时间
```

**关键子函数调用链**:
- `validateBarcode()` → mainwindow.cpp:281 — 解析 `|` 分隔内容，比对条码
- `validateMaterialCode()` → mainwindow.cpp:2780 — 料号白名单/黑名单/确认流程
- `moveLogFile()` → mainwindow.cpp:3123 — 文件移动+重复检测+溢出检测
- `writeTimeRecord()` → mainwindow.cpp:573 — 写入时间统计记录
- `getNextDuplicateNumber()` → mainwindow.cpp:525 — 正则匹配 `条码+序号.log`，返回 max+1

### 4.5 工单管理

#### 4.5.1 手动添加工单

**实现函数**: `onAddWorkOrder()` → `mainwindow.cpp:1608`

```
用户点击"添加工单"按钮
    ↓
QInputDialog::getText() 输入工单号
    ↓
readWorkOrderTotalFromNumFile() 从 workordersRootPath/<orderNumber>/NUM.txt 读取总数
    ↓
创建 WorkOrder 对象，统计已完成数
    ↓
classifyWorkOrder() 分类到未完成/已完成表格
    ↓
updateWorkorderConfig() 更新 workorder.ini（可选）
    ↓
sendNewWorkOrderToCentral() 通过 WebSocket 发送到中控
    ↓
checkAndCreateOrderFolder() 确保归档子目录存在
```

#### 4.5.2 NUM.txt 读写

**读取**: `readWorkOrderTotalFromNumFile()` → `mainwindow.cpp:3752`
- 路径: `workordersRootPath/<orderNumber>/NUM.txt`
- 支持网络路径，手动构造避免 QDir 问题

**写入**: `writeWorkOrderTotalToNumFile()` → `mainwindow.cpp:3811`
- 使用文件锁 `tryLockConfigFile() / unlockConfigFile()` (`.lock` 锁文件) 防止多机并发写入冲突
- 最多重试 50 次，每次间隔 100ms

#### 4.5.3 工单计数统计

- **`countLogFiles()`** → mainwindow.cpp:1914 — 统计工单文件夹根目录 log 文件数
- **`countAllLogFilesInOrderFolder()`** → mainwindow.cpp:1924 — 统计所有子目录（includeOverlog 参数控制是否包含 overlog）
- **`updateWorkOrderCount()`** → mainwindow.cpp:2032 — 更新内存中工单的 completedCount，触发溢出检查
- **`syncAllWorkOrderCounts()`** → mainwindow.cpp:2104 — 由 syncTimer(30s) 驱动，同步所有工单计数，并触发 `maybeStartAsyncSizeValidation()`

#### 4.5.4 大小校验（异步）

**触发**: `maybeStartAsyncSizeValidation()` → `mainwindow.cpp:2166`
- 每 syncValidationInterval 个 syncTimer 周期触发一次（默认 20×30s = 10分钟）
- 使用 `QtConcurrent::run()` 异步执行，`std::atomic<bool>` 保证单飞
- 校验逻辑: 读取拷贝机记录文件 → 解析 .gob 文件路径 → 在 GobRoute1/2 上获取 .gob 文件大小 → 与本地 log 文件大小比对
- 结果通过 `onSizeValidationFinished(errorMessages)` 信号在 UI 线程处理，弹窗报告不一致项

#### 4.5.5 双表格管理

**实现函数**: `setupDualTableUI()` → mainwindow.cpp:1138, `createDualTableLayout()` → mainwindow.cpp:1183

界面分为两个 QTableWidget:
- **`incompleteTableWidget`**: 未完成工单（剩余数 > 0 且未停止计数）
- **`completedTableWidget`**: 已完成工单（剩余数 = 0 或已停止计数）

每个表格6列: 工单号 | 总数量 | 已完成 | 失败数量 | 剩余数量 | 操作

**分类函数**: `classifyWorkOrder()` → mainwindow.cpp:5137
**状态变更检测**: `checkWorkOrderStatusChange()` → mainwindow.cpp:5295

#### 4.5.6 工单持久化

- **保存**: `saveWorkOrders()` → mainwindow.cpp:3541 — 退出时将 workOrders 序列化为 `saved_workorders.json`
- **加载询问**: `askToLoadSavedWorkOrders()` → mainwindow.cpp:3581 — 启动时弹出对话框询问用户是否加载
- **选择加载**: `WorkOrderSelectDialog` → 用户勾选要加载的工单 → `loadSelectedWorkOrders()` → mainwindow.cpp:3650
- **文件路径**: `getWorkOrdersFilePath()` → mainwindow.cpp:3747，返回 `QDir::current().filePath("saved_workorders.json")`

### 4.6 料号验证系统

**核心函数**: `validateMaterialCode()` → mainwindow.cpp:2780

支持文件新格式：`工单号|条码|料号描述`（第三字段为料号信息）。

**白名单/黑名单存储**:
- 白名单文件: `archivePath/<orderNumber>/material_whitelist.json`
- 黑名单文件: `archivePath/<orderNumber>/material_blacklist.json`

操作函数:
| 函数 | 位置 | 说明 |
|------|------|------|
| `loadOrderMaterialWhitelist()` | :2485 | 从 JSON 文件加载白名单 |
| `saveOrderMaterialWhitelist()` | :2530 | 保存白名单到 JSON 文件 |
| `loadOrderMaterialBlacklist()` | :2566 | 从 JSON 文件加载黑名单 |
| `saveOrderMaterialBlacklist()` | :2611 | 保存黑名单到 JSON 文件 |
| `showMaterialCodeConfirmationDialog()` | :2676 | 全屏弹窗让用户确认/拒绝新料号 |
| `isMaterialPendingConfirmation()` | :5378 | 检查料号是否已有等待中的确认对话框 |
| `moveFileToMaterialCache()` | :5384 | 待确认状态将文件移到料号缓存目录 |
| `moveFilesFromCacheToArchive()` | :5410 | 确认后将缓存文件移到正式归档 |
| `cleanupMaterialCache()` | :5468 | 拒绝后清理料号缓存目录 |
| `getMaterialCachePath()` | :5498 | 返回 `<materialCachePath>/<orderNumber>/<materialCode>` |

**料号确认回调**（`showMaterialCodeConfirmationDialog` 中设置）:
- 用户确认 → 料号加入白名单，缓存文件移到正式归档
- 用户拒绝 → 料号加入黑名单，相关缓存文件移到 errorPath
- 用户关闭（ESC 兜底） → 等同于拒绝

**待确认工单队列**: `pendingOrderQueue` + `pendingOrderTimer`(5s)
- `processPendingOrders()` → mainwindow.cpp:2757 — 定期检查并处理等待确认的工单
- 调用 `moveFilesFromCacheToArchive()` 处理已确认料号的缓存文件

### 4.7 WebSocket 中控通信

**初始化**: `setupWebSocket()` → mainwindow.cpp:4073

连接所有 WebSocketClient 信号到 MainWindow 的对应槽函数。

**通信流程**:

```
用户点击"连接中控" → onConnectButtonClicked()
    ↓
m_webSocketClient->connectToServer()
    ↓
onWebSocketConnected()
    ↓
sendAllWorkOrdersToCentral()  发送全量工单 JSON
    ↓
m_sentWorkOrdersAfterConnect  记录已发送集合

新工单添加 → sendNewWorkOrderToCentral()  仅发送新增工单（防重复）
```

**自动重连机制**:
- `setupAutoConnectTimer()` → mainwindow.cpp:5328
- `autoConnectTimer` 每 30 秒触发 `checkAndAutoConnect()` → mainwindow.cpp:5348
- 只有当连接断开且存在已加载工单时，才自动发起重连

**JSON 数据格式**:
```json
{
  "machineId": "Machine01",
  "orders": [
    {
      "orderNumber": "WO2024001",
      "totalCount": 100,
      "completedCount": 45,
      "failCount": 2,
      "remainingCount": 53
    }
  ]
}
```

构造函数: `createWorkOrdersJson()` → mainwindow.cpp:4122, `createSingleWorkOrderJson()` → mainwindow.cpp:4142

### 4.8 报警与通知系统

#### 4.8.1 溢出报警

**调用链**: `moveLogFile()` → `showOverflowAlert()` → `OverflowAlertManager::showOverflowAlert()`

- `showOverflowAlert()` (MainWindow 成员) → mainwindow.cpp:4581 — 带5分钟冷却的溢出通知
- `OverflowAlertManager::showOverflowAlert()` → mainwindow.cpp:5520 — 单例管理器的溢出报警，10分钟冷却
- 冷却机制: 同一工单在冷却时间内不重复弹窗
- 问题解决时 (`onOverflowResolved`) 清除冷却，允许立即重新弹窗

#### 4.8.2 通知管理

| 函数 | 位置 | 说明 |
|------|------|------|
| `shouldShowNotification()` | :4460 | 检查工单是否在通知冷却期内（5分钟） |
| `recordNotification()` | :4476 | 记录通知时间戳 |
| `showNotification()` | :4483 | 创建全屏通知对话框 |
| `cleanupExpiredNotifications()` | :4520 | 清理超过限制的历史通知记录（上限100条） |
| `shouldShowOverflowAlert()` | :4558 | 检查溢出报警冷却状态（5分钟） |
| `recordOverflowAlert()` | :4574 | 记录溢出报警时间 |

#### 4.8.3 工单完成通知与异常报警

- `showCompletionNotification()` → mainwindow.cpp:5224 — 非阻塞全屏弹窗，工单完成时通知
- `showAnomalyAlert()` → mainwindow.cpp:5246 — 工单状态异常时报警（当前实现为空，被注释）

### 4.9 拷贝控制功能

**入口**: `onStartCopyClicked()` → mainwindow.cpp:4319

创建 `CopyControlDialog` 并以模态方式运行。

**CopyControlDialog 功能**:
- 工单号写入 `E:/OrderNumber.txt` → Windows 消息通知厂商程序读取
- 开始/停止/工作模式切换 → 通过 `PostMessage` 发送自定义 Windows 消息到厂商窗口

**工作模式映射**:
| 界面选项 | 枚举值 | WM_COPY_WORK_MODE LPARAM |
|----------|--------|--------------------------|
| 快速拷贝 | QUICK_COPY | 0 |
| 快速拷贝+比对 | QUICK_COPY_COMPARE | 1 |
| 全盘拷贝 | FULL_COPY | 2 |
| 全盘拷贝+比对 | FULL_COPY_COMPARE | 3 |
| 制作镜像 | MAKE_MAP | 6 |
| 镜像拷贝 | MAP_COPY | 7 |

### 4.10 日志文件格式转换

- **`ensureLogExtension()`** → mainwindow.cpp:489 — 将 `.txt` 文件重命名为 `.log`（跳过 `TimeCount*.txt`）
- **`convertTxtToLogInOrderFolder()`** → mainwindow.cpp:2312 — 批量转换工单文件夹下所有 `.txt` 为 `.log`（跳过 `TimeCount*.txt`）

### 4.11 网络路径支持

程序全面支持 Windows 网络路径（UNC 路径如 `\\server\share`），涉及以下工具函数:

| 函数 | 位置 | 说明 |
|------|------|------|
| `isNetworkPath()` | :4326 | 判断是否为网络路径（支持 `\\`, `//`, `\` 开头及 Unix 风格） |
| `checkNetworkPathAccessibility()` | :4336 | 检查网络路径可访问性 |
| `createNetworkFolder()` | :4365 | 创建网络路径文件夹（逐级创建） |

> **背景**: QSettings 读取 INI 文件时会将 `\\` 转换为 `\`，因此路径判断逻辑需要兼容处理各种变体格式。

---

## 五、UI 布局

```
┌─────────────────────────────────────────────────────┐
│                    MainWindow 标题栏                  │
│         "MSI-工单管理系统 - 机器号: XXX"              │
├─────────────────────────────────────────────────────┤
│  未完成工单                                          │
│  ┌─────────┬──────┬──────┬──────┬──────┬──────────┐ │
│  │ 工单号   │ 总数  │ 完成  │ 失败  │ 剩余  │  操作     │ │
│  ├─────────┼──────┼──────┼──────┼──────┼──────────┤ │
│  │ WO001   │ 100  │  45  │  2   │  53  │ 修改 删除 │ │
│  └─────────┴──────┴──────┴──────┴──────┴──────────┘ │
│  ───────────────── 分割线 ──────────────────────     │
│  已完成工单                                          │
│  ┌─────────┬──────┬──────┬──────┬──────┬──────────┐ │
│  │ 工单号   │ 总数  │ 完成  │ 失败  │ 剩余  │  操作     │ │
│  ├─────────┼──────┼──────┼──────┼──────┼──────────┤ │
│  │ WO002   │  50  │  50  │  0   │  0   │ 修改 删除 │ │
│  └─────────┴──────┴──────┴──────┴──────┴──────────┘ │
│                                                       │
│  [添加工单] [已处理工单] [刷新] [连接中控] [开始拷贝]    │
│                                                       │
│  状态栏: WebSocket 连接状态                            │
└─────────────────────────────────────────────────────┘
```

**操作按钮**（每行）:
- **修改**（橙色）: `onModifyTotalCount()` — 修改工单总量（同步更新 NUM.txt）
- **删除**（红色）: `onDeleteWorkOrder()` — 删除工单，加入 `suppressedAutoSyncOrders`
- **失败+1**（灰蓝色）: `onMarkAsFailed()` — 标记失败数+1
- **查看详情**（右键菜单）: `onViewWorkOrderDetails()` → mainwindow.cpp:5107

---

## 六、定时器总览

| 定时器 | 间隔 | 触发函数 | 作用 |
|--------|------|----------|------|
| `processTimer` | 100ms | `processNextFile()` | 驱动文件处理队列 |
| `syncTimer` | 30s | `syncAllWorkOrderCounts()` | 同步所有工单计数 + 触发大小校验 |
| `queueProgressTimer` | 2s | `updateQueueProgress()` | 更新批量处理进度对话框 |
| `notificationCleanupTimer` | 24h | `cleanupExpiredNotifications()` | 清理过期通知记录 |
| `pendingOrderTimer` | 5s | `processPendingOrders()` | 处理等待料号确认的工单队列 |
| `autoConnectTimer` | 30s | `checkAndAutoConnect()` | 自动重连中控服务器 |
| `cooldownTimer` (OverflowAlertManager) | 60s | `checkCooldownExpired()` | 检查溢出报警冷却状态 |

---

## 七、外部依赖与配置文件清单

### 7.1 配置文件

| 文件 | 格式 | 用途 | 生成方式 |
|------|------|------|----------|
| `config.ini` | INI | 主配置（全部路径、机器ID、中控地址等） | 首次启动自动生成默认配置 |
| `workorder.ini` | INI | 工单配置（旧方式，可选） | 外部提供 |
| `saved_workorders.json` | JSON | 工单持久化 | 程序退出时自动保存 |
| `<WorkordersRoot>/<工单号>/NUM.txt` | 纯文本 | 单个工单的总数量 | 外部提供 |

### 7.2 料号相关文件（程序自动管理）

| 文件 | 路径 |
|------|------|
| 料号白名单 | `<archivePath>/<orderNumber>/material_whitelist.json` |
| 料号黑名单 | `<archivePath>/<orderNumber>/material_blacklist.json` |
| 料号缓存 | `<materialCachePath>/<orderNumber>/<materialCode>/` |

### 7.3 运行时数据文件

| 文件 | 路径 | 内容 |
|------|------|------|
| TimeCount 文件 | `<archivePath>/<orderNumber>/TimeCount<MachineID>.txt` | 每条记录的时间统计 |
| 应用日志 | `<程序目录>/logs/app_<日期>.log` | 带时间戳和级别的运行日志 |

### 7.4 Qt 依赖

- Qt6 (或 Qt5) Widgets
- Qt6 (或 Qt5) WebSockets
- Qt6 (或 Qt5) Concurrent

### 7.5 平台 API 依赖

- Windows API (`FindWindowW`, `PostMessage`, `SetForegroundWindow`, `EnumWindows`, `GetClassNameW`, `GetWindowTextW` 等) — 用于拷贝控制和窗口激活
- `QFile::birthTime()` — 获取文件创建时间

---

## 八、数据流架构图

```
                        ┌──────────────┐
                        │  config.ini  │
                        └──────┬───────┘
                               │ 读取
                               ▼
┌──────────┐    监控     ┌──────────┐   WebSocket   ┌──────────┐
│ 厂商拷贝机 │◄──────────►│ MainWindow│◄────────────►│ 中控服务器 │
│(Windows消息)│ 拷贝控制   │   (核心)  │  工单信息上报  │          │
└──────────┘            └────┬─────┘              └──────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
    ┌─────▼─────┐     ┌──────▼──────┐     ┌──────▼──────┐
    │ watchPath │     │ archivePath │     │duplicatePath│
    │ (新文件)   │     │ (归档)      │     │  (重复文件)  │
    └───────────┘     └──────┬──────┘     └─────────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
        ┌─────▼─────┐  ┌────▼────┐  ┌──────▼──────┐
        │ 根目录     │  │completed│  │  overlog    │
        │ (<=总数)   │  │ (<=总数) │  │  (超出总数)  │
        └───────────┘  └─────────┘  └─────────────┘
```

---

## 九、关键业务规则

1. **文件格式**: log 文件内容为 `工单号|条码`（旧格式）或 `工单号|条码|料号描述`（新格式）
2. **条码验证**: 文件名（不含扩展名）必须等于文件内容中的条码字段
3. **重复检测**: 检查根目录 → completed → overlog 三级，已有同名文件则旧文件标记为重复（`条码+N.log` 命名）移入 duplicatePath
4. **溢出判断**: 实时统计根目录 + completed 的 log 文件数量，当 ≥ totalCount 时新文件移入 overlog，触发出报警
5. **料号验证**: 新料号需要人工确认（白名单/黑名单机制），确认前文件缓存到 MaterialCache
6. **冷却机制**: 同一工单的通知有冷却时间（5分钟），溢出报警有冷却时间（10分钟），避免重复骚扰
7. **文件锁保护**: `writeWorkOrderTotalToNumFile()` 使用 `.lock` 文件 + 重试机制（最多50次）防止多机并发写入 NUM.txt

---

## 十、编译与运行

### 编译要求
- CMake 3.16+
- Qt 6 (或 Qt 5) 带 Widgets, WebSockets, Concurrent 模块
- C++17 编译器 (MinGW 64-bit 或 MSVC)
- Windows SDK

### 构建命令
```bash
mkdir build && cd build
cmake .. -DCMAKE_PREFIX_PATH=<Qt安装路径>
cmake --build .
```

### 运行前准备
1. 确保 `config.ini` 存在于程序目录（或由程序自动生成默认配置，再手动修改）
2. 确保 `netuse.bat` 存在于程序目录（可选，用于映射网络驱动器）
3. 确保所有配置的路径可访问（本地路径自动创建，网络路径需预先配置）
4. 确保 `workordersRootPath/<工单号>/NUM.txt` 文件存在（工单总数字段来源）
