# copyssd 从零开始开发指南

> 本文档假设你回到了该程序最初开发的时间节点，功能需求不变，但这次你拥有一位有经验的导师在旁边指导你每一步应该怎么做。本文不会替你写代码，而是告诉你**在什么阶段应该思考什么、做出什么决策、按什么顺序推进**。

---

## 目录

1. [开始之前：理解你要做什么](#一-开始之前理解你要做什么)
2. [第一阶段：让骨架立起来（1-2 天）](#二-第一阶段让骨架立起来)
3. [第二阶段：核心流水线——处理一个文件（3-5 天）](#三-第二阶段核心流水线处理一个文件)
4. [第三阶段：工单管理（2-3 天）](#四-第三阶段工单管理)
5. [第四阶段：异常处理体系（3-4 天）](#五-第四阶段异常处理体系)
6. [第五阶段：用户界面（3-4 天）](#六-第五阶段用户界面)
7. [第六阶段：网络通信（2-3 天）](#七-第六阶段网络通信)
8. [第七阶段：拷贝机控制（1-2 天）](#八-第七阶段拷贝机控制)
9. [第八阶段：打磨与上线准备（3-5 天）](#九-第八阶段打磨与上线准备)
10. [编码规范](#十编码规范)
11. [常见错误与应对](#十一常见错误与应对)

---

## 一、开始之前：理解你要做什么

### 1.1 先写下来，不要急着打开 IDE

在新建 `main.cpp` 之前，先用自然语言把需求写清楚。这不是浪费时间——这是在**避免你写着写着发现方向错了**。

#### 核心业务是什么？

用一段话描述你的程序要做什么：

> 工厂产线上，每完成一次 SSD 拷贝操作，拷贝机会在指定的监控文件夹下生成一个日志文件（格式：`工单号|条码` 或 `工单号|条码|料号描述`）。程序需要自动检测这些文件，验证条码正确性，然后将文件按工单号归档到对应的文件夹中。如果发现同名文件已存在则标记为重复，如果文件数超过工单总量则标记为溢出并报警。同时，程序需要向中控服务器上报每个工单的进度，并支持向拷贝机发送控制指令。

#### 输入是什么，输出是什么？

| 输入 | 来源 | 说明 |
|------|------|------|
| 日志文件 | `watchPath` 文件夹 | 内容为 `工单号\|条码[\|料号]` |
| 工单总量 | `workordersRootPath/<工单号>/NUM.txt` | 单个整数 |
| 配置文件 | `config.ini` | 所有路径、机器ID、中控地址等 |

| 输出 | 去向 | 说明 |
|------|------|------|
| 归档文件 | `archivePath/<工单号>/` | 正常文件 |
| 溢出文件 | `archivePath/<工单号>/overlog/` | 超出总量的文件 |
| 重复文件 | `duplicatePath/<工单号>/条码+N.log` | 同名重复文件 |
| 不匹配文件 | `mismatchPath/` | 条码不一致的文件 |
| 时间记录 | `archivePath/<工单号>/TimeCount<machineId>.txt` | 每条处理记录 |
| 工单状态 | 中控服务器（WebSocket） | JSON 格式 |
| 运行日志 | `logs/app_YYYY-MM-DD.log` | 带时间戳的日志 |

#### 有哪些边界情况？

在开始编码前就想清楚这些"如果..."：

- 如果文件正在被拷贝机写入（文件被占用），怎么办？
- 如果同一个条码的文件到达了两次，怎么办？
- 如果工单文件夹在网络上（UNC 路径），怎么处理？
- 如果程序崩溃重启，怎么恢复？
- 如果多台机器同时处理同一个工单，怎么防止冲突？
- 如果中控服务器重启了，怎么自动重连？
- 如果工单总量后来被修改了（NUM.txt 变了），怎么办？

你现在不需要有所有答案，但**在开始编码前把这些问题列出来**，可以防止你在后期才发现架构根本装不下这些场景。

### 1.2 画出数据流

在纸上画出数据怎么流动，比直接敲代码高效得多：

```
拷贝机生成文件
      │
      ▼
  watchPath ──(QFileSystemWatcher 监控)──► 文件处理队列
                                                │
                                                ▼
                                     ┌─ 读文件内容 ──────────┐
                                     │                       │
                                     ▼                       ▼
                              条码匹配验证              料号验证
                                     │                 (白/黑名单)
                                     ▼                       │
                              解析工单号/条码                 │
                                     │                       │
                                     ▼                       ▼
                              ┌─ moveLogFile ──────────────────┐
                              │                                │
                              ▼                                ▼
                        检查重复文件                      检查是否溢出
                              │                                │
                              ▼                                ▼
                    移入 duplicatePath       移入 overlog + 触发报警
                              
                    无重复且未溢出 → 移入 archivePath/<工单号>/
                              │
                              ▼
                    写入 TimeCount<machineId>.txt
                              │
                              ▼
                    更新工单计数 → 通知 UI 刷新
                              │
                              ▼
                    通过 WebSocket 上报中控
```

画出这张图后，你就能识别出**哪些是独立的模块**：
- 文件读取与解析
- 条码验证
- 料号验证
- 文件移动 + 重复检测 + 溢出判断
- 时间记录
- 工单管理
- UI 展示
- 中控通信

这就是你需要的**类的雏形**。

### 1.3 确定技术方案

| 决策 | 选择 | 理由 |
|------|------|------|
| UI 框架 | Qt Widgets | 成熟的桌面 UI 框架，有 QFileSystemWatcher、QWebSocket 等现成组件 |
| 文件监控 | QFileSystemWatcher | Qt 内置，跨平台，有 `directoryChanged` 信号 |
| 网络通信 | QWebSocket | Qt 内置 WebSocket 客户端 |
| 配置格式 | INI 文件 | 简单，非技术人员也能编辑，QSettings 原生支持 |
| 构建系统 | CMake | Qt 官方推荐，跨平台 |
| 跨平台 | 不需要 | 只跑 Windows，可以用 Windows API 做拷贝机控制 |

---

## 二、第一阶段：让骨架立起来

**目标**：一个能编译运行的空窗口 + 日志系统 + 配置读取。不处理任何业务。

### 2.1 项目结构从第一天就规划好

不要在根目录堆所有文件。从现在开始就用分层目录：

```
copyssd/
├── CMakeLists.txt
├── src/
│   ├── main.cpp                    # 程序入口
│   ├── app/
│   │   ├── mainwindow.h            # 主窗口（只做 UI 协调）
│   │   └── mainwindow.cpp
│   ├── core/                       # 核心业务逻辑
│   │   ├── fileprocessor.h         # 文件处理（第二阶段加）
│   │   ├── fileprocessor.cpp
│   │   ├── filemover.h             # 文件移动（第二阶段加）
│   │   ├── filemover.cpp
│   │   ├── ordermanager.h          # 工单管理（第三阶段加）
│   │   └── ordermanager.cpp
│   ├── validation/                 # 验证相关
│   │   ├── barcodevalidator.h
│   │   ├── barcodevalidator.cpp
│   │   ├── materialvalidator.h
│   │   └── materialvalidator.cpp
│   ├── network/                    # 网络通信
│   │   ├── websocketclient.h
│   │   └── websocketclient.cpp
│   ├── config/                     # 配置管理
│   │   ├── configmanager.h
│   │   └── configmanager.cpp
│   ├── logging/                    # 日志系统
│   │   ├── logger.h
│   │   └── logger.cpp
│   ├── ui/                         # UI 组件
│   │   ├── fullscreenalertdialog.h
│   │   ├── fullscreenalertdialog.cpp
│   │   ├── copycontroldialog.h
│   │   ├── copycontroldialog.cpp
│   │   ├── workorderselectdialog.h
│   │   └── workorderselectdialog.cpp
│   └── utils/                      # 纯工具函数
│       ├── pathutils.h
│       ├── pathutils.cpp
│       ├── fileutils.h
│       └── fileutils.cpp
├── tests/                          # 测试代码
│   ├── CMakeLists.txt
│   ├── tst_barcodevalidator.cpp
│   ├── tst_fileprocessor.cpp
│   └── ...
└── resources/
    ├── resources.qrc
    └── ldb.ico
```

**命名约定**：
- `src/core/` — 核心业务逻辑，不依赖 UI
- `src/ui/` — 只放和界面显示相关的代码
- `src/utils/` — 纯函数，不依赖任何业务对象
- `src/config/` — 配置管理
- `tests/` — 测试代码，文件以 `tst_` 开头

**关键原则**：`core/` 里的代码**不允许** `#include` `ui/` 里的任何头文件。业务逻辑不依赖 UI。UI 可以依赖业务逻辑，业务逻辑不能依赖 UI。

### 2.2 第一步：CMakeLists.txt

先让编译系统跑通：

```cmake
cmake_minimum_required(VERSION 3.16)
project(copyssd VERSION 0.1 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTORCC ON)

find_package(Qt6 REQUIRED COMPONENTS Widgets WebSockets Concurrent Test)

# 收集所有源文件（按目录分组，清晰易读）
set(CONFIG_SOURCES
    src/config/configmanager.h
    src/config/configmanager.cpp
)

set(LOGGING_SOURCES
    src/logging/logger.h
    src/logging/logger.cpp
)

set(UI_SOURCES
    src/ui/fullscreenalertdialog.h
    src/ui/fullscreenalertdialog.cpp
    src/ui/copycontroldialog.h
    src/ui/copycontroldialog.cpp
    src/ui/workorderselectdialog.h
    src/ui/workorderselectdialog.cpp
)

set(CORE_SOURCES
    src/core/fileprocessor.h
    src/core/fileprocessor.cpp
    src/core/filemover.h
    src/core/filemover.cpp
    src/core/ordermanager.h
    src/core/ordermanager.cpp
)

# ... 其他类似

qt_add_executable(copyssd
    src/main.cpp
    src/app/mainwindow.h src/app/mainwindow.cpp
    ${CONFIG_SOURCES}
    ${LOGGING_SOURCES}
    ${UI_SOURCES}
    ${CORE_SOURCES}
    ${VALIDATION_SOURCES}
    ${NETWORK_SOURCES}
    ${UTILS_SOURCES}
    resources/resources.qrc
)

target_include_directories(copyssd PRIVATE ${CMAKE_SOURCE_DIR}/src)
target_link_libraries(copyssd PRIVATE
    Qt6::Widgets Qt6::WebSockets Qt6::Concurrent
)

# 启用测试
enable_testing()
add_subdirectory(tests)
```

### 2.3 第二步：ConfigManager（先于 MainWindow）

**不要先写 MainWindow**。MainWindow 依赖配置，所以先让配置系统可用。

```cpp
// src/config/configmanager.h
#pragma once

#include <QObject>
#include <QString>
#include <QSettings>

class ConfigManager : public QObject
{
    Q_OBJECT

public:
    explicit ConfigManager(const QString& configFilePath = "config.ini",
                           QObject* parent = nullptr);

    // 机器信息
    QString machineId() const { return m_machineId; }

    // 路径
    QString watchPath()       const { return m_watchPath; }
    QString archivePath()     const { return m_archivePath; }
    QString duplicatePath()   const { return m_duplicatePath; }
    QString mismatchPath()    const { return m_mismatchPath; }
    QString errorPath()       const { return m_errorPath; }
    QString materialCachePath() const { return m_materialCachePath; }
    QString workordersRootPath() const { return m_workordersRootPath; }
    QString gobRoute1()       const { return m_gobRoute1; }
    QString gobRoute2()       const { return m_gobRoute2; }
    QString copyMachineLogPath() const { return m_copyMachineLogPath; }

    // 中控配置
    QString centralServerIP()   const { return m_centralIP; }
    int     centralServerPort() const { return m_centralPort; }

    // 同步配置
    int syncValidationInterval() const { return m_syncValidationInterval; }

    // 路径构造方法（消除重复——文档第四章提到的）
    QString buildArchivePath(const QString& orderNumber) const;

    // 验证配置是否完整
    bool isValid() const;
    QStringList validatePaths() const;  // 返回有问题的路径列表

signals:
    void configLoaded();
    void configError(const QString& message);

private:
    void load();
    void createDefault();
    bool ensurePathExists(const QString& path);

    QString m_configFilePath;

    QString m_machineId;
    QString m_watchPath;
    QString m_archivePath;
    QString m_duplicatePath;
    QString m_mismatchPath;
    QString m_errorPath;
    QString m_materialCachePath;
    QString m_workordersRootPath;
    QString m_gobRoute1;
    QString m_gobRoute2;
    QString m_copyMachineLogPath;
    QString m_centralIP;
    int     m_centralPort = 8080;
    int     m_syncValidationInterval = 20;
};
```

**要点**：
- 配置项全部 `private`，通过 getter 访问。外界不需要知道配置是来自 INI 文件还是环境变量
- `buildArchivePath(orderNumber)` 这一类**路径构造方法放在 ConfigManager 里**——因为路径怎么拼接是配置层的职责，不是业务层的职责
- 构造函数接受 `configFilePath` 参数，测试时可以传入临时文件路径

### 2.4 第三步：Logger（独立于一切）

日志系统不依赖任何人，最先写：

```cpp
// src/logging/logger.h
#pragma once

#include <QString>
#include <QMutex>

class Logger {
public:
    enum Level { Debug, Info, Warning, Error };

    static Logger& instance();

    void log(Level level, const QString& message);
    void setLogFile(const QString& path);

private:
    Logger() = default;
    // ... 实现略，你现有的 Logger 代码结构是好的
};

// 便捷宏
#define LOG_DEBUG(msg)   Logger::instance().log(Logger::Debug, msg)
#define LOG_INFO(msg)    Logger::instance().log(Logger::Info, msg)
#define LOG_WARNING(msg) Logger::instance().log(Logger::Warning, msg)
#define LOG_ERROR(msg)   Logger::instance().log(Logger::Error, msg)
```

你现有的 Logger 实现（logger.cpp）的代码质量是可以的——单例、线程安全、日志轮转。放在 `src/logging/` 下即可。

### 2.5 第四步：main.cpp（最小化）

```cpp
// src/main.cpp
#include <QApplication>
#include <QIcon>
#include "config/configmanager.h"
#include "logging/logger.h"
#include "app/mainwindow.h"

int main(int argc, char *argv[])
{
    QApplication app(argc, argv);
    app.setWindowIcon(QIcon(":/ldb.ico"));

    // 初始化日志
    Logger::instance().setLogFile("logs/app.log");
    LOG_INFO("程序启动");

    // 加载配置
    ConfigManager config;
    if (!config.isValid()) {
        LOG_ERROR("配置无效，程序退出");
        return 1;
    }

    // 创建主窗口，注入配置
    MainWindow window(&config);
    window.show();

    return app.exec();
}
```

注意这行：`MainWindow window(&config);`

MainWindow **不自己读 config.ini**。配置从外部注入。这是"依赖注入"思想的最基本体现。

### 2.6 第五步：MainWindow（此时只是一个空壳）

```cpp
// src/app/mainwindow.h
#pragma once

#include <QMainWindow>

class ConfigManager;

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    explicit MainWindow(ConfigManager* config, QWidget* parent = nullptr);

private:
    void setupUI();

    ConfigManager* m_config;
};

// src/app/mainwindow.cpp
#include "app/mainwindow.h"
#include "config/configmanager.h"

MainWindow::MainWindow(ConfigManager* config, QWidget* parent)
    : QMainWindow(parent)
    , m_config(config)
{
    setWindowTitle(QString("MSI-工单管理系统 - 机器号: %1").arg(config->machineId()));
    resize(1200, 800);
    setupUI();
}

void MainWindow::setupUI()
{
    // 目前只放一个 QLabel 占位
    QLabel* label = new QLabel("程序已启动，等待功能开发...", this);
    label->setAlignment(Qt::AlignCenter);
    setCentralWidget(label);
}
```

**阶段检查点**：编译通过，运行后显示一个带正确标题的空白窗口，日志文件正常写入。如果有任何问题，在这里解决。不要带着问题进入下一阶段。

### 2.7 你现在就可以写第一个测试了

在 `tests/` 下创建测试项目：

```cpp
// tests/tst_configmanager.cpp
#include <QTest>
#include <QTemporaryFile>
#include "config/configmanager.h"

class TestConfigManager : public QObject
{
    Q_OBJECT

private slots:
    void defaultValues()
    {
        // 给一个不存在的配置文件路径
        ConfigManager config("nonexistent_config.ini");
        // 验证默认值
        QCOMPARE(config.machineId(), QString("DEFAULT"));
    }

    void customConfig()
    {
        // 创建一个临时配置文件
        QTemporaryFile tempFile;
        tempFile.open();
        tempFile.write("[Machine]\nID=TEST001\n");
        tempFile.flush();

        ConfigManager config(tempFile.fileName());
        QCOMPARE(config.machineId(), QString("TEST001"));
    }
};

QTEST_MAIN(TestConfigManager)
```

**养成习惯**：每写完一个独立的模块，就给它写一两个测试。不是"写完所有功能再补测试"，而是**和业务代码同步写**。

---

## 三、第二阶段：核心流水线——处理一个文件

**目标**：程序能够检测到新文件、读取内容、解析条码、归档文件。这是整个系统的核心。只做"正常路径"，暂不处理重复、溢出、料号验证。

### 3.1 先写工具函数（并先写测试）

把无依赖的纯函数独立出来，优先实现和测试：

```cpp
// src/utils/pathutils.h
#pragma once

#include <QString>

namespace PathUtils {

// 判断是否为网络路径
bool isNetworkPath(const QString& path);

// 安全地拼接路径（自动处理 UNC 路径的复杂性）
QString joinPath(const QString& base, const QString& sub);

// 获取文件分隔符（网络路径用反斜杠，本地用 Qt 默认）
QString pathSeparator(const QString& path);

} // namespace PathUtils
```

```cpp
// tests/tst_pathutils.cpp
#include <QTest>
#include "utils/pathutils.h"

class TestPathUtils : public QObject
{
    Q_OBJECT

private slots:
    void isNetworkPath_data()
    {
        QTest::addColumn<QString>("path");
        QTest::addColumn<bool>("expected");

        QTest::newRow("UNC双反斜杠")  << "\\\\server\\share" << true;
        QTest::newRow("UNC单反斜杠")  << "\\server\\share"   << true;
        QTest::newRow("本地盘符")     << "D:/Watch"          << false;
        QTest::newRow("本地路径")     << "C:\\data"          << false;
        QTest::newRow("空字符串")     << ""                  << false;
    }

    void isNetworkPath()
    {
        QFETCH(QString, path);
        QFETCH(bool, expected);
        QCOMPARE(PathUtils::isNetworkPath(path), expected);
    }

    void joinPath_networkPath()
    {
        QString result = PathUtils::joinPath("\\\\server\\share", "WO001");
        QCOMPARE(result, QString("\\\\server\\share\\WO001"));
    }

    void joinPath_localPath()
    {
        QString result = PathUtils::joinPath("D:/Archive", "WO001");
        QCOMPARE(result, QString("D:/Archive/WO001"));
    }
};

QTEST_APPLESS_MAIN(TestPathUtils)
```

> **关于 `_data()` 模式**：Qt Test 的 `_data()` 函数让你定义一个测试的多个输入/输出组合。上面的 `isNetworkPath_data()` 定义了 5 组输入和期望输出，`isNetworkPath()` 会对每一组执行一次测试。这是减少重复测试代码的技巧。

### 3.2 实现 BarcodeValidator

这是一个纯逻辑类，不依赖文件系统、网络或 UI：

```cpp
// src/validation/barcodevalidator.h
#pragma once

#include <QString>

class BarcodeValidator
{
public:
    // 解析文件内容，返回结果
    struct ParseResult {
        bool   valid;
        QString orderNumber;
        QString barcode;
        QString materialDescription;  // 新格式第三字段，旧格式为空
        QString errorMessage;
    };

    // 验证文件名中的条码 与 文件内容中的条码 是否一致
    static bool isBarcodeMatch(const QString& filePath, const QString& fileContent);

    // 解析文件内容
    static ParseResult parse(const QString& filePath, const QString& fileContent);
};
```

**为什么是静态方法**：BarcodeValidator 没有状态，不需要实例化。如果以后需要 mock（模拟），再改为虚方法。

**先写测试，再写实现**：

```cpp
// tests/tst_barcodevalidator.cpp
#include <QTest>
#include "validation/barcodevalidator.h"

class TestBarcodeValidator : public QObject
{
    Q_OBJECT

private slots:
    void parse_oldFormat()
    {
        auto result = BarcodeValidator::parse(
            "D:/Watch/ABC123.log",
            "WO001|ABC123"
        );
        QVERIFY(result.valid);
        QCOMPARE(result.orderNumber, QString("WO001"));
        QCOMPARE(result.barcode, QString("ABC123"));
        QVERIFY(result.materialDescription.isEmpty());
    }

    void parse_newFormat()
    {
        auto result = BarcodeValidator::parse(
            "D:/Watch/BAR456.log",
            "WO002|BAR456|SAMSUNG_FLASH"
        );
        QVERIFY(result.valid);
        QCOMPARE(result.orderNumber, QString("WO002"));
        QCOMPARE(result.barcode, QString("BAR456"));
        QCOMPARE(result.materialDescription, QString("SAMSUNG_FLASH"));
    }

    void parse_insufficientFields()
    {
        auto result = BarcodeValidator::parse(
            "D:/Watch/TEST.log",
            "WO001"   // 只有工单号，没有条码
        );
        QVERIFY(!result.valid);
        QVERIFY(!result.errorMessage.isEmpty());
    }

    void parse_emptyContent()
    {
        auto result = BarcodeValidator::parse("D:/Watch/TEST.log", "");
        QVERIFY(!result.valid);
    }

    void isBarcodeMatch_true()
    {
        QVERIFY(BarcodeValidator::isBarcodeMatch(
            "D:/Watch/ABC123.log", "WO001|ABC123"));
    }

    void isBarcodeMatch_false()
    {
        QVERIFY(!BarcodeValidator::isBarcodeMatch(
            "D:/Watch/ABC123.log", "WO001|XYZ789"));
    }
};

QTEST_APPLESS_MAIN(TestBarcodeValidator)
```

写完测试，运行——**全部红色（失败）**。然后写实现代码，直到全部绿色（通过）。这叫 **TDD（测试驱动开发）** 的最简形式：红 → 绿 → 重构。

### 3.3 实现 FileProcessor——核心流水线

```cpp
// src/core/fileprocessor.h
#pragma once

#include <QObject>
#include <QQueue>
#include <QTimer>
#include <QFileSystemWatcher>

class ConfigManager;
class OrderManager;
class BarcodeValidator;    // 注意：不依赖 UI

class FileProcessor : public QObject
{
    Q_OBJECT

public:
    explicit FileProcessor(ConfigManager* config,
                           OrderManager* orderManager,
                           QObject* parent = nullptr);

    void start();   // 开始监控
    void stop();    // 停止监控

    // 处理单个文件（公开，方便测试）
    bool processFile(const QString& filePath);

signals:
    void fileProcessed(const QString& orderNumber, const QString& barcode);
    void fileRejected(const QString& filePath, const QString& reason);
    void progressUpdated(int queued, int processed);

private slots:
    void onDirectoryChanged(const QString& path);
    void onProcessNextFile();

private:
    // 读取文件内容（带重试）
    bool readFileWithRetry(const QString& filePath, QString& content);

    // 归档文件
    bool archiveFile(const QString& sourcePath,
                     const QString& orderNumber,
                     const QString& barcode);

    ConfigManager*     m_config;
    OrderManager*      m_orderManager;
    QFileSystemWatcher* m_watcher;
    QTimer*            m_processTimer;
    QQueue<QString>    m_fileQueue;
    bool               m_isProcessing = false;
};
```

```cpp
// src/core/fileprocessor.cpp（核心逻辑）
bool FileProcessor::processFile(const QString& filePath)
{
    // 1. 读文件内容
    QString content;
    if (!readFileWithRetry(filePath, content)) {
        emit fileRejected(filePath, "无法读取文件");
        return false;
    }

    // 2. 验证条码匹配
    if (!BarcodeValidator::isBarcodeMatch(filePath, content)) {
        handleMismatch(filePath);
        emit fileRejected(filePath, "条码不匹配");
        return false;
    }

    // 3. 解析内容
    auto parsed = BarcodeValidator::parse(filePath, content);
    if (!parsed.valid) {
        emit fileRejected(filePath, parsed.errorMessage);
        return false;
    }

    // 4. 归档文件
    if (!archiveFile(filePath, parsed.orderNumber, parsed.barcode)) {
        emit fileRejected(filePath, "文件归档失败");
        return false;
    }

    // 5. 通知成功
    emit fileProcessed(parsed.orderNumber, parsed.barcode);
    return true;
}
```

**这个阶段的 FileProcessor 只做 5 件事**，不包含重复检测、溢出判断、料号验证。那些在后续阶段（第四阶段）再加。当前阶段的目标是让最简单的"读文件→验证→归档"流程跑通。

### 3.4 实现最小的 OrderManager

此时 OrderManager 只需要最基本的功能：

```cpp
// src/core/ordermanager.h
#pragma once

#include <QObject>
#include <QMap>

struct WorkOrder {
    QString orderNumber;
    int     totalCount    = 0;
    int     completedCount = 0;
    int     failedCount   = 0;

    int remainingCount() const {
        return qMax(0, totalCount - completedCount - failedCount);
    }
};

class ConfigManager;

class OrderManager : public QObject
{
    Q_OBJECT

public:
    explicit OrderManager(ConfigManager* config, QObject* parent = nullptr);

    bool addOrder(const QString& orderNumber);
    bool hasOrder(const QString& orderNumber) const;
    WorkOrder* getOrder(const QString& orderNumber);

    void incrementCompleted(const QString& orderNumber);
    void updateCounts(const QString& orderNumber);  // 从文件夹重新统计

    QList<WorkOrder*> allOrders() const;

signals:
    void orderAdded(const QString& orderNumber);
    void orderUpdated(const QString& orderNumber);
    void orderCompleted(const QString& orderNumber);

private:
    int readTotalFromNumFile(const QString& orderNumber) const;

    ConfigManager* m_config;
    QMap<QString, WorkOrder*> m_orders;
};
```

### 3.5 阶段检查点

此时你应该能够：
1. 手动放一个 `.log` 文件到 watchPath
2. 程序自动检测、读取、验证条码、归档到 archivePath
3. 文件处理的结果通过信号通知给 MainWindow
4. 工单计数正确更新
5. `ctest` 运行后，BarcodeValidator 和 PathUtils 的测试全部通过

---

## 四、第三阶段：工单管理

**目标**：能够手动添加/删除工单，UI 显示工单列表，工单数据持久化。

### 4.1 扩展 OrderManager

加入：
- 从 JSON 文件保存/加载工单列表
- 添加/删除/修改工单的接口
- 自动同步计数（定时从文件夹重新统计）

```cpp
class OrderManager : public QObject
{
    // ... 第二阶段已有的方法 ...

    // 第三阶段新增
    bool removeOrder(const QString& orderNumber);
    bool updateTotalCount(const QString& orderNumber, int newTotal);
    void syncAllCounts();  // 由定时器驱动

    bool saveToFile();
    bool loadFromFile();

    struct PersistedOrder {
        QString orderNumber;
        int totalCount;
    };
    QList<PersistedOrder> loadPersistedOrders() const;
};
```

### 4.2 MainWindow 连接 UI

这一阶段 MainWindow 开始真正承担 UI 协调的角色：

```cpp
// MainWindow 中的信号连接（不是写在构造函数里的一大坨）
void MainWindow::connectSignals()
{
    // 文件处理 → UI 更新
    connect(m_fileProcessor, &FileProcessor::fileProcessed,
            this, [this](const QString& order, const QString& barcode) {
        m_orderManager->incrementCompleted(order);
        updateTableRow(order);
        statusBar()->showMessage(
            QString("已处理: %1 (条码: %2)").arg(order, barcode), 5000);
    });

    // 工单变化 → UI 刷新
    connect(m_orderManager, &OrderManager::orderUpdated,
            this, &MainWindow::updateTableRow);

    // 用户操作 → 业务层
    connect(m_addButton, &QPushButton::clicked,
            this, &MainWindow::onUserAddOrder);
}
```

关键：**UI 层不和业务层直接耦合**。MainWindow 通过信号槽连接二者，但它自己不包含文件解析或工单计数的逻辑。

### 4.3 阶段检查点

- 可以手动输入工单号添加
- 表格正确显示工单的完成/剩余情况
- 程序重启后可以选择恢复上次的工单
- OrderManager 的测试覆盖了添加、删除、加载、保存

---

## 五、第四阶段：异常处理体系

**目标**：处理重复文件、溢出文件、料号验证、条码不匹配等异常场景。这是让程序从"玩具"变成"生产可用"的关键阶段。

### 5.1 为什么异常处理要单独一个阶段

因为异常处理的逻辑复杂，加在"能跑通正常路径"之上比一上来就写所有分支要容易得多。你现在有一个跑通的正常流程，你可以：
- 在正常流程的适当位置插入异常处理的钩子
- 每个异常场景可以独立开发和测试
- 不会因为异常处理的 bug 影响正常流程

### 5.2 扩展 FileMover

在第二阶段，`archiveFile` 是直接移动文件。现在扩展为处理各种情况：

```cpp
// src/core/filemover.h
#pragma once

#include <QObject>
#include <QString>

class ConfigManager;

// 枚举目标位置
enum class FileDestination {
    Normal,      // 归档根目录
    Completed,   // completed/ 子目录
    Overflow,    // overlog/ 子目录
    Duplicate    // duplicatePath/
};

class FileMover : public QObject
{
    Q_OBJECT

public:
    struct MoveResult {
        bool success;
        FileDestination destination;
        QString finalPath;
        int copyCount;         // 重复次数（1 = 首次）
        QString failReason;
    };

    explicit FileMover(ConfigManager* config, QObject* parent = nullptr);

    MoveResult moveToArchive(const QString& sourcePath,
                              const QString& orderNumber,
                              const QString& barcode,
                              int currentTotalCount,
                              int currentCompletedCount);

signals:
    void overflowDetected(const QString& orderNumber);
    void duplicateDetected(const QString& orderNumber,
                           const QString& barcode,
                           int copyCount);

private:
    bool isDuplicate(const QString& destPath) const;
    bool isOverflow(int completed, int total) const;
    int  getNextDuplicateSeq(const QString& barcode,
                             const QString& orderNumber) const;
    bool ensureDirectoryExists(const QString& path);

    ConfigManager* m_config;
};
```

**测试 FileMover 各种场景**：

```cpp
// tests/tst_filemover.cpp
class TestFileMover : public QObject
{
    Q_OBJECT

private:
    QTemporaryDir m_watchDir;
    QTemporaryDir m_archiveDir;
    QTemporaryDir m_dupDir;
    ConfigManager* m_config;
    FileMover* m_mover;

    void init() override
    {
        m_config = new ConfigManager(this);
        m_config->setArchivePath(m_archiveDir.path());
        m_config->setDuplicatePath(m_dupDir.path());
        m_mover = new FileMover(m_config, this);
    }

private slots:
    void normalMove()
    {
        // 创建源文件
        QString src = m_watchDir.path() + "/TEST001.log";
        QFile f(src);
        f.open(QIODevice::WriteOnly);
        f.write("WO001|TEST001");
        f.close();

        auto result = m_mover->moveToArchive(src, "WO001", "TEST001",
                                              100, 0);  // 总数100，已完成0
        QVERIFY(result.success);
        QCOMPARE(result.destination, FileDestination::Normal);
        QVERIFY(QFile::exists(m_archiveDir.path() + "/WO001/TEST001.log"));
    }

    void overflowMove()
    {
        // 模拟已完成数 = 总数（溢出条件）
        QString src = m_watchDir.path() + "/OVERFLOW_001.log";
        QFile f(src);
        f.open(QIODevice::WriteOnly);
        f.write("WO001|OVERFLOW_001");
        f.close();

        auto result = m_mover->moveToArchive(src, "WO001", "OVERFLOW_001",
                                              10, 10);  // 总数10，已完成10
        QVERIFY(result.success);
        QCOMPARE(result.destination, FileDestination::Overflow);
        QVERIFY(QFile::exists(m_archiveDir.path() + "/WO001/overlog/OVERFLOW_001.log"));
    }

    void duplicateMove()
    {
        // 先在目标位置放一个同名文件
        QDir(m_archiveDir.path()).mkpath("WO001");
        QFile existing(m_archiveDir.path() + "/WO001/DUP_001.log");
        existing.open(QIODevice::WriteOnly);
        existing.write("existing");
        existing.close();

        QString src = m_watchDir.path() + "/DUP_001.log";
        QFile f(src);
        f.open(QIODevice::WriteOnly);
        f.write("new");
        f.close();

        auto result = m_mover->moveToArchive(src, "WO001", "DUP_001",
                                              100, 0);
        QVERIFY(result.success);
        // 旧文件被移到重复目录
        QVERIFY(QFile::exists(m_dupDir.path() + "/WO001/DUP_001+1.log"));
        // 新文件被放到归档目录
        QVERIFY(QFile::exists(m_archiveDir.path() + "/WO001/DUP_001.log"));
    }
};
```

### 5.3 实现 MaterialValidator

```cpp
// src/validation/materialvalidator.h
#pragma once

#include <QObject>
#include <QStringList>
#include <QMap>

class ConfigManager;

class MaterialValidator : public QObject
{
    Q_OBJECT

public:
    enum class Result {
        Accepted,             // 在白名单中，直接通过
        Rejected,             // 在黑名单中，直接拒绝
        PendingConfirmation,  // 新料号，需要确认
        Error                 // 处理出错
    };

    explicit MaterialValidator(ConfigManager* config, QObject* parent = nullptr);

    Result validate(const QString& orderNumber,
                    const QString& barcode,
                    const QString& materialCode,
                    const QString& filePath);

    void confirmMaterial(const QString& orderNumber, const QString& materialCode);
    void rejectMaterial(const QString& orderNumber, const QString& materialCode);

signals:
    void materialConfirmed(const QString& orderNumber, const QString& materialCode);
    void materialRejected(const QString& orderNumber, const QString& materialCode);
    void confirmationNeeded(const QString& orderNumber, const QString& barcode,
                            const QString& materialCode);

private:
    bool loadWhitelist(const QString& orderNumber);
    bool loadBlacklist(const QString& orderNumber);
    void saveWhitelist(const QString& orderNumber);
    void saveBlacklist(const QString& orderNumber);

    ConfigManager* m_config;
    QMap<QString, QStringList> m_whitelistCache;
    QMap<QString, QStringList> m_blacklistCache;
};
```

**注意**：MaterialValidator 只判断料号是否合法，**不负责弹窗**。弹窗是 UI 层的事。它通过 `confirmationNeeded` 信号通知外界"需要用户确认"。这样你在写集成测试时，连接的是测试代码中的 lambda 而不是真实弹窗。

### 5.4 把异常处理集成到 FileProcessor

```cpp
bool FileProcessor::processFile(const QString& filePath)
{
    // ... 前面不变 ...

    auto parsed = BarcodeValidator::parse(filePath, content);
    if (!parsed.valid) {
        emit fileRejected(filePath, parsed.errorMessage);
        return false;
    }

    // 新增：料号验证
    auto matResult = m_materialValidator->validate(
        parsed.orderNumber, parsed.barcode,
        parsed.materialDescription, filePath);

    switch (matResult) {
    case MaterialValidator::Accepted:
        break;  // 继续处理

    case MaterialValidator::Rejected:
        emit fileRejected(filePath, "料号在黑名单中");
        return false;

    case MaterialValidator::PendingConfirmation:
        // 文件已被 MaterialValidator 移到缓存目录
        emit fileRejected(filePath, "等待料号确认");
        return false;

    case MaterialValidator::Error:
        emit fileRejected(filePath, "料号验证出错");
        return false;
    }

    // 新增：用 FileMover 处理归档（含重复和溢出检测）
    auto moveResult = m_fileMover->moveToArchive(
        filePath, parsed.orderNumber, parsed.barcode,
        m_orderManager->getTotalCount(parsed.orderNumber),
        m_orderManager->getCompletedCount(parsed.orderNumber));

    if (!moveResult.success) {
        emit fileRejected(filePath, moveResult.failReason);
        return false;
    }

    // ... 后续处理 ...
}
```

### 5.5 阶段检查点

- 重复文件正确移动到 duplicatePath，序号递增
- 溢出文件移动到 overlog，触发报警
- 新料号暂停处理并需要人工确认
- 白名单料号自动通过，黑名单料号自动拒绝
- 每种异常场景都有对应的集成测试

---

## 六、第五阶段：用户界面

**目标**：构建完整的 UI——双表格、操作按钮、报警弹窗、状态栏。

### 6.1 UI 开发的原则

1. **UI 类只做展示和事件转发**。`MainWindow` 的每个按钮点击槽函数不超过 5 行——只调用业务对象的方法
2. **UI 组件可以独立开发和预览**。比如 `FullScreenAlertDialog` 可以先单独测试它的显示效果，不连接任何业务逻辑
3. **先画原型再写代码**。用手绘或 Qt Designer 画好布局，确定每个控件的位置和大小

### 6.2 开发顺序

```text
1. 单表格显示工单列表（先只显示数据，不加操作按钮）
2. 操作按钮列（修改、删除、失败+1）→ 每个按钮连接 OrderManager
3. 双表格拆分（未完成 / 已完成）
4. 添加工单对话框
5. FullScreenAlertDialog（先做最简单的"确认+关闭"版本）
6. 报警与 UI 连接（溢出报警、完成通知）
7. 状态栏 + 进度条
8. 右键菜单
```

每一步都是**可测试、可验证的增量**。不要试图一次性写完所有 UI。

### 6.3 MainWindow 的构造函数应该多简洁

```cpp
MainWindow::MainWindow(ConfigManager* config,
                       FileProcessor* processor,
                       OrderManager* orderManager,
                       WebSocketManager* wsManager,
                       QWidget* parent)
    : QMainWindow(parent)
    , m_config(config)
    , m_processor(processor)
    , m_orderManager(orderManager)
    , m_wsManager(wsManager)
{
    setupUI();        // 创建控件、设置布局——纯 UI
    connectSignals(); // 连接信号槽——纯关联
    // 没有任何业务逻辑、没有任何文件操作、没有任何网络请求
}
```

> **原则**：构造函数只做"组装"，不做"工作"。工作由 `start()` 方法或外部调用来触发。

---

## 七、第六阶段：网络通信

**目标**：WebSocket 连接中控服务器，上报工单状态，支持自动重连。

### 7.1 隔离网络层

WebSocketClient 保持现有的结构（你现有的 `websocketclient.h/cpp` 设计是合理的），只需要注意：

```cpp
// WebSocketClient 不应该知道 MainWindow 的存在
// 它只知道自己连接/断开/发送数据/接收数据
// 所有对外通知都通过信号

class WebSocketClient : public QObject
{
    Q_OBJECT

public:
    // 构造函数接受地址，而不是自己去读 config.ini
    explicit WebSocketClient(const QString& serverIP,
                              int port,
                              QObject* parent = nullptr);

    void connectToServer();
    void disconnectFromServer();
    void sendMessage(const QJsonObject& data);

    bool isConnected() const;

signals:
    void connected();
    void disconnected();
    void messageReceived(const QJsonObject& data);
    void errorOccurred(const QString& error);
};
```

### 7.2 中控管理器

```cpp
// src/network/centralserverclient.h
class CentralServerClient : public QObject
{
    Q_OBJECT

public:
    explicit CentralServerClient(ConfigManager* config, QObject* parent = nullptr);

    void start();  // 启动自动连接
    void reportAllOrders(const QMap<QString, WorkOrder*>& orders);
    void reportSingleOrder(const QString& orderNumber, const WorkOrder* order);

signals:
    void connectionStatusChanged(const QString& status);

private:
    void tryReconnect();

    WebSocketClient* m_wsClient;
    QTimer*          m_reconnectTimer;
    ConfigManager*   m_config;
    QSet<QString>    m_reportedOrders;  // 防重复上报
};
```

---

## 八、第七阶段：拷贝机控制

**目标**：通过 Windows 消息向厂商拷贝机发送控制指令。

### 8.1 隔离平台相关代码

Windows API 调用（FindWindow、PostMessage）应该被封装在一个专用类中，不污染其他代码：

```cpp
// src/platform/windowmessenger.h  （只在 Windows 上编译）
#pragma once

#ifdef Q_OS_WIN

#include <QObject>
#include <windows.h>

class WindowMessenger : public QObject
{
    Q_OBJECT

public:
    struct TargetWindow {
        QString className;
        QString windowTitle;
    };

    explicit WindowMessenger(QObject* parent = nullptr);

    bool findWindow(const TargetWindow& target);
    bool postMessage(UINT message, WPARAM wParam = 0, LPARAM lParam = 0);
    QString lastError() const;

private:
    HWND m_hwnd = nullptr;
    QString m_lastError;
};

#endif // Q_OS_WIN
```

### 8.2 拷贝控制对话框

```cpp
// src/ui/copycontroldialog.h
class CopyControlDialog : public QDialog
{
    Q_OBJECT

public:
    explicit CopyControlDialog(WindowMessenger* messenger,
                                const QString& orderNumberFilePath,
                                QWidget* parent = nullptr);

private slots:
    void onSendOrderNumber();
    void onStartCopy();
    void onStopCopy();
    void onSetWorkMode(int mode);

private:
    WindowMessenger* m_messenger;
    QString m_orderNumberFilePath;
    QLineEdit* m_orderEdit;
    QComboBox* m_modeCombo;
};
```

---

## 九、第八阶段：打磨与上线准备

### 9.1 错误恢复

- 程序崩溃重启后，未处理的文件不丢失（仍然在 watchPath 中）
- 处理到一半的文件不会处于"半归档"状态
- 网络断开重连后，数据不丢失，自动补报

### 9.2 监控与诊断

- 每条关键操作都有日志
- 日志包含足够上下文（文件名、工单号、条码），方便排查产线问题
- 异常情况（网络断开、路径不可访问、文件无法读取）有明确日志

### 9.3 配置验证

- 启动时全面检查所有路径是否可用
- 缺失的配置项给出清晰提示（而不是程序偷偷用默认值）
- 网络路径不可访问时给出诊断建议

### 9.4 上线检查清单

```text
□ 正常文件处理：放文件 → 正确归档 → 计数更新
□ 重复文件：同名文件 → 旧文件移到 duplicatePath → 序号正确
□ 溢出文件：总数满了 → 新文件进 overlog → 报警弹窗
□ 条码不匹配：内容条码 ≠ 文件名 → 移到 mismatchPath
□ 文件被占用：拷贝机正在写 → 重试后成功
□ 网络路径：UNC 路径下所有操作正常
□ 配置缺失：缺少路径 → 启动时报清晰错误
□ 工单持久化：重启后可选恢复
□ 中控连接：自动连接/断线重连/数据上报
□ 日志：所有操作有迹可循
□ ctest 全部通过
```

---

## 十、编码规范

### 10.1 命名规范

```cpp
// 类名：大驼峰
class FileProcessor { };
class WebSocketClient { };

// 方法名：小驼峰
void processFile();
bool isDuplicate() const;

// 成员变量：m_ 前缀
QString m_watchPath;
QTimer* m_processTimer;

// 静态常量：全大写+下划线
static const int MAX_RETRY_COUNT = 10;

// 宏：全大写+下划线
#define LOG_INFO(msg) ...
```

### 10.2 文件组织规范

- 一个头文件只声明一个公开类
- 类的声明和实现分两个文件（`.h` 和 `.cpp`）
- 只在一个 `.cpp` 中使用的辅助函数放在匿名命名空间或 `private static` 中
- 在多个 `.cpp` 中使用的工具函数放在 `utils/` 下的命名空间中

### 10.3 注释规范

```cpp
// 不要写这种注释（说"做了什么"——代码已经说清楚了）：
// 读取文件内容
QString content = readFile(filePath);

// 考虑写这种注释（说"为什么这样做"——代码说不清楚）：
// QSettings 读取 INI 时会将 \\ 转换为 \，
// 因此需要同时处理两种前缀格式来识别网络路径
if (path.startsWith("\\\\") || path.startsWith("\\")) {
    // ...
}

// 模块级别的职责划分可以用简短注释说明
// FileProcessor: 负责日志文件的读取、解析、验证流程
// 不负责文件移动（见 FileMover）和料号判断（见 MaterialValidator）
```

### 10.4 提交规范

每完成一个独立的功能点就提交一次：

```bash
git commit -m "添加 PathUtils 工具类：网络路径判断与安全拼接"
git commit -m "添加 BarcodeValidator：文件内容解析与条码验证"
git commit -m "添加 FileProcessor：核心文件处理流水线"
git commit -m "添加 FileMover 重复检测和溢出判断"
```

不要攒了 3 天的代码一个 commit。小步提交便于回溯，也降低冲突风险。

---

## 十一、常见错误与应对

### 11.1 "我先让功能跑起来，结构以后再说"

这是最常见的陷阱。后果是：功能跑起来了，但你舍不得改了（"万一改坏了呢"），于是每一次加新功能都在原来的屎山上继续堆。

**应对**：在第二阶段就建好目录结构，每个类放在该在的位置。多花 30 分钟建目录结构，节省后期 30 小时的重构时间。

### 11.2 "这个功能太简单，不需要测试"

BarcodeValidator 只有 20 行代码，但它是整个文件处理流程的第一道关卡。如果它把 `parts[1]` 写成 `parts[0]`，后面所有逻辑全部出错。

**应对**：简单的函数更容易测试，也更应该测试——写一个测试只要 3 分钟，排查一个由此引发的产线 bug 可能要 3 小时。

### 11.3 "我先在 MainWindow 里写着，以后需要再拆"

"以后"永远不会来。一旦 MainWindow 变成了巨型类，拆分它的成本比你从一开始就分开要高出 10 倍。

**应对**：从一开始就告诉自己：MainWindow 只能做 UI 组装和信号路由。一旦发现自己在 MainWindow 里写文件处理逻辑，就停下手，想想该把这个逻辑放在哪个已有的类里，或者是否需要新建一个类。

### 11.4 "我看不懂别人的设计模式，直接用最土的办法写"

不需要一开始就懂所有设计模式。但需要遵守一个最基础的规则：**一个类只做一件事，一个方法只做一件事**。这是所有设计模式的根基。

如果你发现一个方法的名字需要用"和"字连接（比如 `parseAndValidateAndMove`），就说明它做了不止一件事，应该拆分。

### 11.5 "重构风险太大，万一改坏了产线停摆"

这正是**测试**的价值所在。如果你没有测试，确实不应该重构——因为你的代码是"靠运气在运行"。一旦有了测试，重构就有了安全网：改完代码跑测试，全部通过就说明行为没有改变。

这就是为什么第一阶段就要搭建测试框架。

---

## 总结：开发路线图一览

```
第一阶段（1-2天）
  □ CMake 骨架 + 目录结构
  □ ConfigManager + 测试
  □ Logger
  □ MainWindow 空壳 + 测试
  → 里程碑：程序能跑，配置可加载，测试框架就绪

第二阶段（3-5天）
  □ PathUtils + 测试
  □ BarcodeValidator + 测试
  □ FileProcessor（正常路径）+ 集成测试
  □ OrderManager（最小版本）
  → 里程碑：手动放文件能被正确归档

第三阶段（2-3天）
  □ OrderManager 完整功能 + 测试
  □ MainWindow 工单表格
  □ 工单持久化（JSON 保存/加载）
  → 里程碑：可以管理多个工单，重启可恢复

第四阶段（3-4天）
  □ FileMover（重复检测、溢出判断）+ 测试
  □ MaterialValidator + 测试
  □ 异常场景集成测试
  → 里程碑：所有异常场景处理正确

第五阶段（3-4天）
  □ 双表格 UI
  □ FullScreenAlertDialog
  □ 操作按钮 + 右键菜单
  □ 状态栏 + 进度条
  → 里程碑：UI 完整可用

第六阶段（2-3天）
  □ WebSocketClient（不耦合 MainWindow）
  □ CentralServerClient（自动重连、数据上报）
  → 里程碑：中控通信正常

第七阶段（1-2天）
  □ WindowMessenger（封装 Windows API）
  □ CopyControlDialog
  → 里程碑：可以控制拷贝机

第八阶段（3-5天）
  □ 错误恢复
  □ 配置验证增强
  □ 上线检查清单逐项验证
  □ 日志审查（确保所有关键路径有日志）
  → 里程碑：具备上线条件
```

**总计预估**：约 18-28 个工作日（一个半月左右），对于一个独立开发者来说这是合理的节奏。

如果你想在第 10 天就有一个"能跑"的版本给产线试用，可以这样调整：

- **第 1-5 天**：第一阶段 + 第二阶段（核心流水线），此时已经可以处理正常文件
- **第 6-10 天**：第四阶段的重复检测和溢出判断。此时产线可以试用（手动添加工单、自动归档、异常报警）
- **第 11-28 天**：逐步完善 UI、中控通信、拷贝控制、料号验证

关键原则仍然是：**每个阶段结束时，程序都是可用状态**。不要处于"半成品跑不起来"的状态超过 1 天。
